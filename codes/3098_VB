<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br> <br>Public Class ComboBoxKeystrokes<br>    Dim elem As String()<br>    Dim dist As Integer(,)<br> <br>    Private Function getDist(ByVal from As Integer, ByVal toA As Integer) As Integer<br>        Dim firstChar As Char<br>        Dim i, c As Integer<br> <br>        If from = toA Then Return 0<br>        firstChar = Char.ToLower(elem(toA).Chars(0))<br>        i = from<br>        Do<br>            Do<br>                i += 1<br>                If i &gt;= elem.Length Then i = 0<br>            Loop Until firstChar = Char.ToLower(elem(i).Chars(0))<br>            c += 1<br>            If i = toA Then Return c<br>        Loop<br>    End Function<br>    Public Function minimumKeystrokes(ByVal elements As String()) As Integer<br>        Dim i, j, k, ret As Integer<br>        Dim keepGoing As Boolean<br>        elem = elements<br>        ReDim dist(elem.Length - 1, elem.Length - 1)<br> <br>        For i = 0 To elem.Length - 1<br>            For j = 0 To elem.Length - 1<br>                dist(i, j) = getDist(i, j)<br>            Next<br>        Next<br>        Do<br>            keepGoing = False<br>            For i = 0 To elem.Length - 1<br>                For j = 0 To elem.Length - 1<br>                    For k = 0 To elem.Length - 1<br>                        If dist(i, j) &gt; dist(i, k) + dist(k, j) Then<br>                            dist(i, j) = dist(i, k) + dist(k, j)<br>                            keepGoing = True<br>                        End If<br>                    Next<br>                Next<br>            Next<br>        Loop While keepGoing<br>        For i = 0 To elem.Length - 1<br>            For j = 0 To elem.Length - 1<br>                ret = Math.Max(ret, dist(i, j))<br>            Next<br>        Next<br>        Return ret<br>    End Function<br>End Class<br></td>