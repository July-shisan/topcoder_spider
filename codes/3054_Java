<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.*;<br>import java.util.regex.*;<br> <br>public class Hex {<br>  <br>  ArrayList al = new ArrayList();<br>  <br>  void place(int x, int y, char c) {<br>    x+=bx;<br>    y+=by;<br>  while (al.size()&lt;=x) {<br>    al.add(new StringBuffer());<br>  }<br>  StringBuffer sb = (StringBuffer)al.get(x);<br>  while (sb.length()&lt;=y) {sb.append(' ');}<br>  sb.setCharAt(y,c);<br>}<br> <br>int bx, by;<br>void set(int d, int v) {<br>  bx = d+2*v;<br>  by = 2*d;<br>}<br> <br>public String[] picture(int n, String[] marks) {<br>  ut = System.out;<br>  int ant = marks.length;<br>  for (int i=0; i&lt;n; ++i) {<br>    for (int j=0; j&lt;n; ++j) {<br>      set(i,j);<br>      place(0,1,'_');<br>      place(1,0,'/');<br>      place(1,2,'\\');<br>      place(2,0,'\\');<br>      place(2,2,'/');<br>      place(2,1,'_');<br>    }<br>  }<br>  for (int i=0; i&lt;ant; ++i) {<br>    int d = Integer.parseInt(""+marks[i].charAt(0));<br>    int v = Integer.parseInt(""+marks[i].charAt(1));<br>    set(d,v);<br>    place(1,1,marks[i].charAt(2));<br>  }<br>  int len = al.size();<br>  String[] ans = new String[len];<br>  for (int i=0; i&lt;len; ++i) {<br>    ans[i] = al.get(i).toString();<br>  }<br>  return ans;<br>}<br> <br> <br> <br> <br>java.io.PrintStream ut;<br>void print(int[] a) {<br>  int n = a.length;<br>  for (int i=0; i&lt;n; ++i) {<br>    ut.print(a[i]+" ");<br>  }<br>  ut.println();<br>}<br>void print(String s, int[][] a) {<br>  int n = a.length;<br>  ut.println(s);<br>  for (int i=0; i&lt;n; ++i) {<br>    print(a[i]);<br>  }<br>}<br>int g(Object o) {<br>  return ((Integer)o).intValue();<br>}<br>int[] ints(String[] s) {<br>  int n = s.length;<br>  int[] a = new int[n];<br>  for (int i=0; i&lt;n; ++i) a[i] = Integer.parseInt(s[i]);<br>  return a;<br>}<br>String[] groups(Matcher m) {<br>  int n = m.groupCount();<br>  String[] ans = new String[n];<br>  for (int i=0; i&lt;n; ++i) ans[i] = m.group(i+1);<br>  return ans;<br>}<br>String[] parse(String exp, String s) {<br>  Pattern p = Pattern.compile(exp);<br>  Matcher m = p.matcher(s);<br>  m.find();<br>  return groups(m);<br>}<br>void print(Object[] o) {<br>  ut.println(Arrays.asList(o));<br>}<br> <br>}<br> <br> <br>// Powered by PopsEdit<br></td>