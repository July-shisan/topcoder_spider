<td class="problemText" colspan="8" valign="middle" align="left">
            Class Subway2<br>    Public Function minTime(ByVal length As Integer, ByVal maxAcceleration As Integer, ByVal maxVelocity As Integer) As Double<br>        Dim d As Double<br>        Dim mt As Double<br>        Dim ret As Double<br><br>        mt = (maxVelocity) / maxAcceleration<br><br>        d = 1 / 2 * maxVelocity * maxVelocity / maxAcceleration<br><br>        If d &lt;= length / 2 Then<br>            ret = mt + mt + (length - 2 * d) / maxVelocity<br>        Else<br>            ret = 2 * Math.Sqrt(length / maxAcceleration)<br>        End If<br><br>        Return ret<br>    End Function<br>End Class<br></td>