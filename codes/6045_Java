<td class="problemText" colspan="8" valign="middle" align="left">
            //Leafy tops of root vegetables strike back...<br>import java.util.*;<br>public class CheapestTabComplete {<br><br>    public int getFewest(String[] names, String w) {<br>        Arrays.sort(names);<br>        int[] cost = new int[w.length() + 1];<br>        Arrays.fill(cost, 100000);<br>        cost[0] = 0;<br>        for (int cnt = 0; cnt &lt; w.length(); cnt++) {<br>          cost[cnt + 1] = Math.min(cost[cnt + 1], cost[cnt] + 1);<br>          int num = 0;<br>          for (int j = 0; j &lt; names.length; j++) {<br>            if (names[j].startsWith(w.substring(0, cnt))) {<br>              num++;<br>              if (w.startsWith(names[j])) {<br>                    cost[names[j].length()] = Math.min(cost[names[j].length()], cost[cnt] + num);<br>              }<br>            }<br>          }<br>        }<br>        return cost[w.length()] + 1;<br>    }<br><br><br>}<br><br><br>// Powered by FileEdit<br>// Powered by CodeProcessor<br></td>