<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br> <br>Public Class Decipher<br>    Public Function decipher(ByVal encoded As String(), ByVal frequencyOrder As String) As String()<br>        Dim letter(25), freq(25), i, j, tmp As Integer<br>        Dim ret(encoded.Length - 1) As String<br> <br>        For i = 0 To 25<br>            letter(i) = i<br>        Next<br>        For i = 0 To encoded.Length - 1<br>            For j = 0 To encoded(i).Length - 1<br>                If encoded(i).Chars(j) &lt;&gt; " " Then freq(Asc(encoded(i).Chars(j)) - Asc("A")) += 1<br>            Next<br>        Next<br>        For i = 25 To 0 Step -1<br>            For j = 0 To i - 1<br>                If freq(j) &lt; freq(j + 1) OrElse (freq(j) = freq(j + 1) AndAlso letter(j) &gt; letter(j + 1)) Then<br>                    tmp = freq(j)<br>                    freq(j) = freq(j + 1)<br>                    freq(j + 1) = tmp<br>                    tmp = letter(j)<br>                    letter(j) = letter(j + 1)<br>                    letter(j + 1) = tmp<br>                End If<br>            Next<br>        Next<br>        For i = 0 To encoded.Length - 1<br>            ret(i) = ""<br>            For j = 0 To encoded(i).Length - 1<br>                If encoded(i).Chars(j) = " " Then<br>                    ret(i) &amp;= " "<br>                Else<br>                    ret(i) &amp;= frequencyOrder.Chars(Array.IndexOf(letter, Asc(encoded(i).Chars(j)) - Asc("A")))<br>                End If<br>            Next<br>        Next<br>        Return ret<br>    End Function<br>End Class<br></td>