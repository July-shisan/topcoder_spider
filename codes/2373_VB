<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class EulerianRace<br>    Public Function planRoute(ByVal bridges As String()) As Integer()<br>        Dim r(bridges.Length - 1, bridges.Length - 1) As Boolean<br>        Dim a, b As Integer<br>        For a = 0 To bridges.Length - 1<br>            For b = 0 To bridges.Length - 1<br>                r(a, b) = (bridges(a).Chars(b) = "1"c)<br>            Next<br>        Next<br>        Dim ret As New ArrayList<br>        ret.Add(0)<br>        Do<br>            'get tentative<br>            Dim i As Integer<br>            Dim test As Boolean = False<br>            For i = 0 To bridges.Length - 1<br>                For a = 0 To bridges.Length - 1<br>                    If r(ret(i), a) Then<br>                        test = True<br>                        GoTo BUILD<br>                    End If<br>                Next<br>            Next<br>BUILD:<br>            If test Then<br>                'build tentative<br>                Dim t As New ArrayList<br>                Dim goal As Integer = ret(i)<br>                b = goal<br>                Do<br>                    For a = 0 To bridges.Length - 1<br>                        If r(b, a) Then<br>                            r(b, a) = False<br>                            r(a, b) = False<br>                            t.Add(a)<br>                            b = a<br>                            Exit For<br>                        End If<br>                    Next<br>                Loop Until b = goal<br>                ret.InsertRange(i + 1, t)<br>            Else<br>                Return ret.ToArray(GetType(Integer))<br>            End If<br>        Loop<br>    End Function<br>End Class<br></td>