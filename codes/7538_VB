<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class BasketballStandings<br>  Public Function constructTable(ByVal teams As String()) As String()<br>    array.sort(teams)<br>    Dim numteams As Integer = teams.getupperbound(0)<br>    Dim name(numteams) As String, win(numteams) As Integer, loss(numteams) As Integer, place(numteams) As Integer<br>    Dim totga(numteams) As Integer, wins(numteams) As Double, rank(numteams) As Integer<br>    Dim OutStr(numteams) As String<br>    Dim maxga As Integer = 0<br>    Dim I As Integer, J As Integer<br>    Dim splStr() As String<br>    For I = 0 to numteams<br>      splStr = teams(I).Split<br>      name(I) = splstr(0)<br>      win(I) = cint(splstr(1))<br>      loss(I) = cint(splstr(2))<br>      totga(I) = win(I) + loss(I)<br>      If maxga &lt; totga(I) Then maxga = totga(I)<br>    Next I<br>    For I = 0 to numteams<br>      wins(I) = win(I) + (maxga - totga(I))/2<br>    Next I<br>    Dim BestID As Integer, Best As Double<br>    For I = 1 to numteams + 1<br>      Best = -1<br>      For J = 0 to numteams<br>        <br>        If rank(J) = 0 And wins(J) &gt; Best Then<br>          BestID = J<br>          Best = wins(J)<br>        End If<br>    <br>      Next J<br>      rank(bestid) = I<br>      place(I-1) = bestid<br>    Next I<br>    <br>    For I = 0 to numteams<br>      <br>      OutStr(I) = name(place(I)) &amp; " " &amp; Format(wins(place(0))-wins(place(I)) ,"0.0")<br>      <br>    Next I<br>    <br>    Return OutStr<br>    <br>  End Function<br>End Class<br></td>