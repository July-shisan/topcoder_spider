<td class="problemText" colspan="8" valign="middle" align="left">
            import math,string,itertools,fractions,heapq,collections,re,array,bisect<br> <br>class DivideByZero:<br>    def CountNumbers(self, numbers):<br>        numbers = set(numbers)<br>        last_size = 0<br>        while len(numbers) &gt; last_size:<br>          last_size = len(numbers)<br>          for a,b in itertools.permutations(numbers, 2):<br>            a, b = max(a,b), min(a,b)<br>            numbers.add(a//b)<br>          <br>        return len(numbers)<br> <br> <br>#Powered by KawigiEdit-pf 2.2.2!<br></td>