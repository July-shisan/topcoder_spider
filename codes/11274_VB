<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class GogoXCake<br>    Public Function solve(ByVal cake As String(), ByVal cutter As String()) As String<br>        Dim kh As Integer = cake.Length, kw As Integer = cake(0).Length<br>        Dim uh As Integer = cutter.Length, uw As Integer = cutter(0).Length<br>        Dim rcake(kh - 1, kw - 1) As Boolean<br>        Dim rcutter(uh - 1, uw - 1) As Boolean<br>        For i As Integer = 0 To kh - 1<br>            For j As Integer = 0 To kw - 1<br>                rcake(i, j) = cake(i)(j) = "X"c ' a cake remains<br>            Next<br>        Next<br>        For i As Integer = 0 To uh - 1<br>            For j As Integer = 0 To uw - 1<br>                rcutter(i, j) = cutter(i)(j) = "."c ' there must have been a cake<br>            Next<br>        Next<br>        For x As Integer = 0 To kh - uh<br>            For y As Integer = 0 To kw - uw<br>                ' look for match<br>                For i As Integer = 0 To uh - 1<br>                    For j As Integer = 0 To uw - 1<br>                        If rcake(x + i, y + j) And rcutter(i, j) Then GoTo no_match ' never put cake on cake<br>                    Next<br>                Next<br>                ' match :)<br>                For i As Integer = 0 To uh - 1<br>                    For j As Integer = 0 To uw - 1<br>                        rcake(x + i, y + j) = rcake(x + i, y + j) OrElse rcutter(i, j) ' put cake<br>                    Next<br>                Next<br>no_match:<br>            Next<br>        Next<br>        For i As Integer = 0 To kh - 1<br>            For j As Integer = 0 To kw - 1<br>                If Not rcake(i, j) Then Return "NO" ' someone stole this cake<br>            Next<br>        Next<br>        Return "YES" ' full of cake<br>    End Function<br> <br>End Class<br></td>