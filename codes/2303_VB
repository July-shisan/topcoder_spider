<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class SquareCode<br>    Public Function completeIt(ByVal grille As String()) As String()<br>        Dim s As Integer = grille.Length / 2<br>        Dim x, y As Integer<br>        For x = 0 To s - 1<br>            For y = 0 To s - 1<br>                Dim ss As String = ""<br>                ss &amp;= grille(y).Chars(x)<br>                ss &amp;= grille(x).Chars(2 * s - 1 - y)<br>                ss &amp;= grille(2 * s - 1 - y).Chars(2 * s - 1 - x)<br>                ss &amp;= grille(2 * s - 1 - x).Chars(y)<br>                Select Case Count(ss, "."c)<br>                    Case 0<br>                        grille(y) = grille(y).Remove(x, 1).Insert(x, ".")<br>                    Case 1<br>                    Case Else<br>                        Return New String() {}<br>                End Select<br>            Next<br>        Next<br>        Return grille<br>    End Function<br> <br>    Private Function Count(ByVal s As String, ByVal c As Char) As Integer<br>        Dim a As Integer<br>        Dim ret As Integer = 0<br>        For a = 0 To s.Length - 1<br>            If s.Chars(a) = c Then ret += 1<br>        Next<br>        Return ret<br>    End Function<br>End Class<br></td>