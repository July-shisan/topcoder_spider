<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class InstantRunoff<br>    Public Function outcome(ByVal candidates As String, ByVal ballots As String()) As String<br>        While candidates.Length &gt; 0<br>            Dim h As New Hashtable<br>            Dim a, b As Integer<br>            For b = 0 To ballots.Length - 1<br>                h(ballots(b).Chars(0)) += 1<br>            Next<br>            Dim m As Integer = Integer.MaxValue<br>            For a = 0 To candidates.Length - 1<br>                m = Min(m, h(candidates.Chars(a)))<br>            Next<br>            Dim out As New ArrayList<br>            For a = 0 To candidates.Length - 1<br>                If h(candidates.Chars(a)) &gt; ballots.Length / 2 Then<br>                    Return candidates.Chars(a)<br>                End If<br>                If h(candidates.Chars(a)) = m Then<br>                    out.Add(candidates.Chars(a))<br>                End If<br>            Next<br>            Dim c As Char<br>            For Each c In out<br>                candidates = candidates.Replace(c, "")<br>                For b = 0 To ballots.Length - 1<br>                    ballots(b) = ballots(b).Replace(c, "")<br>                Next<br>            Next<br>        End While<br>        Return ""<br>    End Function<br>End Class<br></td>