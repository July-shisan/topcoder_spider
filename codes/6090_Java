<td class="problemText" colspan="8" valign="middle" align="left">
            //import java.smart.programmer.*;<br>import java.awt.*;<br>import java.math.*;<br>import java.util.regex.*;<br>import static java.util.Arrays.*;<br>import static java.lang.Math.*;<br>import java.awt.geom.*;<br>import java.util.*;<br>import java.text.*;<br> <br>//beware of bugs in the following code; I have only made it pass the examples, not proved it correct.<br> <br>public class VowelLatin<br>{<br>  public String translate(String s)<br>  {<br>    String a = "", b = "";<br>    <br>    String vowel = "aeiouAEIOU";<br>    <br>    for(int i = 0; i &lt; s.length(); i++)<br>    {<br>      char c = s.charAt(i);<br>      <br>      if(vowel.contains(""+c))<br>        b += c;<br>      else<br>        a += c;<br>    }<br>    return a+b;<br>  }<br>  <br>  void print(Object ...o) {for(Object i:o)System.err.print(i+" ");System.err.println();}  <br>  <br>}<br>//powered by good ol' micro$oft notepad<br></td>