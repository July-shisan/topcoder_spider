<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class TestScores<br>        Private mQuestions() As Double<br>        Public Function weightedScore(ByVal questions As Double(), ByVal testScore As Integer) As Double<br>            ReDim mQuestions(questions.GetUpperBound(0))<br> <br>            Dim i, j As Integer<br>            For i = 0 To questions.GetUpperBound(0)<br>                mQuestions(i) = questions(i)<br>            Next<br> <br>            Dim dMean As Double = 0<br>            For i = 0 To questions.GetUpperBound(0)<br>                dMean += questions(i)<br>            Next<br> <br>            Dim dAns(questions.GetUpperBound(0) + 1, questions.GetUpperBound(0) + 1) As Double<br>            For i = 0 To questions.GetUpperBound(0) + 1<br>                For j = 0 To questions.GetUpperBound(0) + 1<br>                    dAns(i, j) = 0<br>                Next<br>            Next<br> <br>            dAns(0, 0) = 1<br> <br>            For i = 1 To questions.GetUpperBound(0) + 1<br>                For j = 0 To i - 1<br>                    dAns(i, j + 1) += dAns(i - 1, j) * questions(i - 1)<br>                    dAns(i, j) += dAns(i - 1, j) * (1 - questions(i - 1))<br>                Next<br>            Next<br> <br>            Dim xSampled As Long = 500<br>            Dim dTotError As Double = 0<br> <br>            For i = 0 To questions.GetUpperBound(0) + 1<br>                dTotError += (dMean - i) ^ 2 * dAns(questions.GetUpperBound(0) + 1, i)<br>            Next<br> <br>            dTotError = System.Math.Sqrt(dTotError)<br> <br>            Dim dAdjScore As Double<br>            dAdjScore = 1000<br>            dAdjScore += (testScore - dMean) / dTotError * 300<br> <br>            Return dAdjScore<br> <br> <br>        End Function<br> <br>        Private Function dOdds(ByVal iScore As Integer) As Double<br> <br>        End Function<br> <br>    End Class<br></td>