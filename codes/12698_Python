<td class="problemText" colspan="8" valign="middle" align="left">
            class PiecewiseLinearFunctionDiv2:<br>    def countSolutions(self, Y, query):<br>        otvet = []<br>        maxf = max(Y)<br>        minf = min(Y)<br>        for q in query:<br>            res = 0<br>            if q&lt;=maxf and q&gt;=minf:<br>                if Y[0] == q:<br>                    res += 1<br>                for i in xrange(1,len(Y)):<br>                    if Y[i] == q and Y[i-1] == q:<br>                        res = -1<br>                        break<br>                    elif Y[i] == q:<br>                        res += 1<br>                    elif q &gt; min(Y[i-1],Y[i]) and q &lt; max(Y[i-1],Y[i]):<br>                        res += 1<br>            otvet.append(res)<br>        return tuple(otvet) #tuple (integer)<br></td>