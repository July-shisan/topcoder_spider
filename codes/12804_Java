<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.Arrays;<br> <br>/**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class AstronomicalRecords {<br>    public int minimalPlanets(int[] A, int[] B) {<br>        int res = A.length + B.length;<br>        int[][] max = new int[A.length + 1][B.length + 1];<br>        for (int p : A)<br>            for (int q : B) {<br>                for (int[] a : max) Arrays.fill(a, 0);<br>                for (int i = 0; i &lt; A.length; ++i)<br>                    for (int j = 0; j &lt; B.length; ++j) {<br>                        if (A[i] * (long) q == B[j] * (long) p)<br>                            max[i + 1][j + 1] = max[i][j] + 1;<br>                        else<br>                            max[i + 1][j + 1] = Math.max(max[i + 1][j], max[i][j + 1]);<br>                    }<br>                res = Math.min(res, A.length + B.length - max[A.length][B.length]);<br>            }<br>        return res;<br>    }<br>}<br></td>