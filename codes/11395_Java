<td class="problemText" colspan="8" valign="middle" align="left">
            /**<br> * Generated by Contest helper plug-in<br> * Actual solution is at the bottom<br> */<br>public class SetMultiples {<br>  public long smallestSubset(long A, long B, long C, long D) {<br>    A = Math.max(A, B / 2 + 1);<br>    long start = A;<br>    long result = 0;<br>    while (start != B + 1) {<br>      long ratio = D / start;<br>      long end = Math.min(D / ratio, B);<br>      long goodStart = Math.min((C + ratio - 1) / ratio, end + 1);<br>      result += Math.max(goodStart - start, 0);<br>      start = end + 1;<br>    }<br>    C = Math.max(C, D / 2 + 1);<br>    result += D - C + 1;<br>    return result;<br>  }<br> <br>}<br> <br> <br> <br> <br>// Powered by FileEdit<br>// Powered by moj 4.16 [modified TZTester]<br>// Powered by CodeProcessor<br></td>