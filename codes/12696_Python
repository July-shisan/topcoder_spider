<td class="problemText" colspan="8" valign="middle" align="left">
            class TeamsSelection:<br>    def simulate(self, preference1, preference2):<br>        p1 = list(preference1)<br>        p2 = list(preference2)<br>        otvet = [0]*len(preference1)<br>        for i in xrange(len(preference1)):<br>            if i%2 == 0:<br>                nextbest = p1.pop(0)<br>                otvet[nextbest-1] = '1'<br>                p2.pop(p2.index(nextbest))<br>            else:<br>                nextbest = p2.pop(0)<br>                otvet[nextbest-1] = '2'<br>                p1.pop(p1.index(nextbest))<br>        return ''.join(otvet)<br></td>