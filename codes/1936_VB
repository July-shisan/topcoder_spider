<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class CongruenceLock<br>    Private Structure pair<br>        Dim val As Integer<br>        Dim n As Integer<br>        Public Sub New(ByVal v As Integer, ByVal nn As Integer)<br>            val = v<br>            n = nn<br>        End Sub<br>    End Structure<br> <br>    Public Function minPushes(ByVal current As Integer, ByVal target As Integer, ByVal buttons As Integer()) As Integer<br>        Dim hit(99999) As Boolean<br>        Dim q As New Queue<br>        hit(current) = True<br>        q.Enqueue(New pair(current, 0))<br>        While q.Count &gt; 0<br>            Dim p As pair = q.Dequeue<br>            If p.val = target Then<br>                Return p.n<br>            End If<br>            Dim a As Integer<br>            For a = 0 To buttons.Length - 1<br>                Dim t As Integer = (p.val + buttons(a)) Mod 100000<br>                If t = target Then Return p.n + 1<br>                If Not hit(t) Then<br>                    hit(t) = True<br>                    q.Enqueue(New pair(t, p.n + 1))<br>                End If<br>            Next<br>        End While<br>        Return -1<br>    End Function<br>End Class<br></td>