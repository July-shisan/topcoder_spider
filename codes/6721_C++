<td class="problemText" colspan="8" valign="middle" align="left">
            #include &lt;iostream&gt;<br>#include &lt;vector&gt;<br>#include &lt;string&gt;<br>#include &lt;map&gt;<br>#include &lt;sstream&gt;<br>#define FOR(i,n) for(int i=0;i&lt;n;i++)<br>#define REP(i,f,t) for(int i=f;i&lt;=t;i++)<br>#define debln(a) cout&lt;&lt; #a &lt;&lt;" : " &lt;&lt; a &lt;&lt; endl;<br>#define ALL(a) a.begin(),a.end()<br>using namespace std;<br> <br>class FriendsTrouble {<br>  public:<br>  vector&lt;vector&lt;int&gt; &gt; v;<br>  map&lt;string,int&gt; m;<br>  int n;<br>  vector&lt;int&gt; g;<br>  void dfs(int vert) {<br>    g[vert] = 0;<br>    FOR(i,v[vert].size()) if(g[ v[vert][i] ] == -1) dfs(v[vert][i]);<br>  }<br>  int divide(vector &lt;string&gt; names, vector &lt;string&gt; friends) {<br>    n = names.size();<br>    g.resize(n,-1);<br>    v.resize(n);<br>    FOR(i,n) m[names[i]] = i;<br>    FOR(i,friends.size()) {<br>      string a,b;<br>      istringstream iss(friends[i]);<br>      iss &gt;&gt; a &gt;&gt; b;<br>      v[m[a]].push_back(m[b]);<br>      v[m[b]].push_back(m[a]);<br>    }<br>    int res(0);<br>    FOR(i,n) if(g[i]==-1) { dfs(i);res++; }<br>    return res;<br>  }<br>};<br> <br>// Powered by PopsEdit<br></td>