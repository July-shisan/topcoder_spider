<td class="problemText" colspan="8" valign="middle" align="left">
            #include &lt;string&gt;<br>#include &lt;vector&gt;<br>#include &lt;algorithm&gt;<br>#include &lt;iostream&gt;<br>#include &lt;cstdlib&gt;<br>#include &lt;cstdio&gt;<br>#include &lt;cmath&gt;<br>using namespace std;<br>typedef vector&lt;int&gt; VI; typedef vector&lt;VI&gt; VVI;<br>typedef vector&lt;string&gt; VS; typedef vector&lt;VS&gt; VVS;<br>typedef signed long long i64; typedef unsigned long long u64;<br><br>VS tokenize(string s, string ch) {<br>  VS ret;<br>  for( int p = 0, p2; p &lt; s.size(); p = p2+1 ) {<br>    p2 = s.find_first_of(ch, p);<br>    if( p2 == -1 ) p2 = s.size();<br>    if( p2-p &gt; 0 ) ret.push_back(s.substr(p, p2-p));<br>  }<br>  return ret;<br>}<br><br>VI tokint(string s, string ch) {<br>  VI ret;<br>  VS t = tokenize(s, ch);<br>  for( int i = 0; i &lt; t.size(); i++ )<br>    ret.push_back(atoi(t[i].c_str()));<br>  return ret;<br>}<br><br>int dx[] = {1, 0, -1, 0};<br>int dy[] = {0, 1, 0, -1};<br><br>class RoadTrip {<br>public:<br>int howMany(vector &lt;string&gt; a) {<br>  int i, j, k, x, y, z, n, d;<br>  int cx, cy, cd;<br>  int ret = 0, cur;<br><br>  for( cx = 0; cx &lt; a[0].size(); cx++ )<br>  for( cy = 0; cy &lt; a.size(); cy++ )<br>  for( cd = 0; cd &lt; 4; cd++ ) {<br>    VVI u(a.size(), VI(a[0].size()));<br>    vector&lt;VVI&gt; ud(a.size(), VVI(a[0].size(), VI(4)));<br>    x = cx; y = cy; d = cd;<br>    cur = 0;<br>    while( x &gt;= 0 &amp;&amp; x &lt; a[0].size() &amp;&amp;<br>         y &gt;= 0 &amp;&amp; y &lt; a.size() &amp;&amp; !ud[y][x][d] ) {<br>      if( !u[y][x] ) cur++;<br>      u[y][x] = 1;<br>      ud[y][x][d] = 1;<br>      if( a[y][x] == 'R' ) d = (d+1)%4;<br>      if( a[y][x] == 'L' ) d = (d+3)%4;<br>      if( a[y][x] == 'B' ) d = (d+2)%4;<br>      x += dx[d]; y += dy[d];<br>    }<br>    ret &gt;?= cur;<br>  }<br>  return ret;<br>}<br>};<br><br><br>// Powered by FileEdit<br></td>