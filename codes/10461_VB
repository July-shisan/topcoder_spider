<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System.Collections<br>Imports System.Collections.Generic<br><br><br>Public Class ToolsBox<br><br>    Public Function countTools(ByVal need As String()) As Integer<br><br>        Dim mylist As IList(Of String) = New List(Of String)<br><br>        For Each str As String In need<br>            Dim strSplit As String()<br>            strSplit = str.Split(" "c)<br>            For i As Integer = 0 To strSplit.Length - 1<br>                If Not mylist.Contains(strSplit(i)) Then<br>                    mylist.Add(strSplit(i))<br>                End If<br>            Next<br><br>        Next<br><br>        Return mylist.Count<br>    End Function<br>End Class<br></td>