<td class="problemText" colspan="8" valign="middle" align="left">
            #include &lt;string&gt;<br>#include &lt;vector&gt;<br>#include &lt;set&gt;<br>#include &lt;map&gt;<br>#include &lt;algorithm&gt;<br>#include &lt;numeric&gt;<br>#include &lt;cstdio&gt;<br>#include &lt;iostream&gt;<br> <br>using namespace std;<br> <br>vector&lt; string &gt; tokenize( string x, string tk ) {<br>  set&lt; char &gt; tks( tk.begin(), tk.end() );<br>  const char *v = x.c_str();<br>  while( tks.count( *v ) )<br>    v++;<br>  vector&lt; string &gt; oot;<br>  while( *v ) {<br>    const char *nd = v;<br>    while( *nd &amp;&amp; !tks.count( *nd ) )<br>      nd++;<br>    oot.push_back( string( v, nd ) );<br>    v = nd;<br>    while( tks.count( *v ) )<br>      v++;<br>  }<br>  return oot;<br>}<br> <br>vector&lt; int &gt; sti( const vector&lt; string &gt; &amp;x ) {<br>  vector&lt; int &gt; oot;<br>  for( int i = 0; i &lt; x.size(); i++ )<br>    oot.push_back( atoi( x[ i ].c_str() ) );<br>  return oot;<br>}<br> <br>#define i64 long long<br> <br>i64 gcd( i64 a, i64 b ) {<br>  if( !a )<br>    return b;<br>  return gcd( b % a, a );<br>}<br> <br>class Traffic {<br>  public:<br>  int time(vector &lt;int&gt; a, int b) {<br>    int spl = 300 / b;<br>    for( int x = 0; x &lt; a.size(); x++ )<br>    a[ x ] *= 2;<br>    int ttime = 0;<br>    ttime += spl;<br>    for( int x = 0; x &lt; a.size(); x++ ) {<br>      while( ( ttime / a[ x ] ) % 2 )<br>      ttime++;<br>      printf( "%d at %d\n", ttime, x );<br>      ttime += spl;<br>    }<br>    return ttime / 2;  <br>  }<br>};<br> <br>// Powered by PopsEdit<br></td>