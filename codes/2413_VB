<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class WordWrap<br>    Public Function justify(ByVal lines As String(), ByVal columnWidth As Integer) As String()<br>        Dim ret As New ArrayList<br>        Dim pool As New ArrayList<br>        Dim a As Integer<br>        For a = 0 To lines.Length - 1<br>            pool.AddRange(lines(a).Split(" "c))<br>        Next<br>        Dim curLine As String = ""<br>        For a = 0 To pool.Count - 1<br>            If curLine.Length + pool(a).ToString.Length + 1 &lt;= columnWidth OrElse curLine.Length = 0 Then<br>                If curLine &lt;&gt; "" Then<br>                    curLine &amp;= " " &amp; pool(a)<br>                Else<br>                    curLine = pool(a)<br>                End If<br>            Else<br>                ret.Add(curLine)<br>                curLine = pool(a)<br>            End If<br>        Next<br>        If curLine &lt;&gt; "" Then<br>            ret.Add(curLine)<br>        End If<br>        Return ret.ToArray(GetType(String))<br>    End Function<br>End Class<br></td>