<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System<br>Imports Microsoft.VisualBasic<br> <br>Public Class ProgressBar<br>  Public Function showProgress(taskTimes As Integer(), tasksCompleted As Integer) As String<br>    Dim TotalTime As Double, CompletedTime As Double<br>    Dim percentComplete As Double<br> <br>    Dim i As Integer<br>    For i = 0 To taskTimes.Length - 1<br>      TotalTime = TotalTime + taskTimes(i)<br>      If i &lt; tasksCompleted  Then _<br>        CompletedTime = CompletedTime + taskTimes(i)<br>    Next<br>    percentComplete = CompletedTime / TotalTime<br>    <br>    showProgress = "".PadLeft(Math.Floor(20 * percentComplete), "#") &amp; "".PadRight(20 - Math.Floor(20 * percentComplete), ".")<br>  End Function<br>End Class<br></td>