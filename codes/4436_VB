<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class PerforatedSheet<br>    Public Function getCenterOfMass(ByVal sheetWidth As Integer, ByVal sheetHeight As Integer, ByVal x As Integer(), ByVal y As Integer(), ByVal width As Integer(), ByVal height As Integer()) As Double()<br>        Dim i As Integer<br>        Dim surface, thisSurface, retx, rety As Long<br> <br>        surface = CLng(sheetWidth) * sheetHeight<br>        retx += sheetWidth * CLng(sheetWidth) * sheetHeight<br>        rety += sheetHeight * CLng(sheetWidth) * sheetHeight<br>        For i = 0 To x.Length - 1<br>            thisSurface = CLng(width(i)) * height(i)<br>            retx -= (2 * x(i) + width(i)) * thisSurface<br>            rety -= (2 * y(i) + height(i)) * thisSurface<br>            surface -= thisSurface<br>        Next<br> <br>        If surface = 0 Then<br>            Return New Double() {}<br>        Else<br>            Return New Double() {retx / (2 * surface), rety / (2 * surface)}<br>        End If<br>    End Function<br>End Class<br></td>