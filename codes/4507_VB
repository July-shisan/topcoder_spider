<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br> <br>Public Class CircleDance<br>    Public Function arrangeDancers(ByVal heights As Integer()) As Integer<br>        Dim max, i, arr(heights.Length - 1) As Integer<br> <br>        Array.Sort(heights)<br>        For i = 0 To heights.Length - 1<br>            If i Mod 2 = 0 Then<br>                arr(i \ 2) = heights(i)<br>            Else<br>                arr(heights.Length - i \ 2 - 1) = heights(i)<br>            End If<br>        Next<br>        For i = 0 To heights.Length - 2<br>            max = Math.Max(max, Math.Abs(arr(i) - arr(i + 1)))<br>        Next<br>        max = Math.Max(max, Math.Abs(arr(0) - arr(heights.Length - 1)))<br>        Return max<br>    End Function<br>End Class<br></td>