<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System.Math<br>Imports System.Text<br>Imports System.Text.RegularExpressions<br>Imports System.IO<br>Imports System.Text.Encoding<br><br> Public Class Parallelograms<br><br><br>             Public Function getDifference(ByVal xA As Integer, ByVal yA As Integer, ByVal xB As Integer, ByVal yB As Integer, ByVal xC As Integer, ByVal yC As Integer) As Double<br><br>            If (yA - yB) / (xA - xB) = (yB - yC) / (xB - xC) OrElse (yA - yB) / (xA - xB) = (yC - yA) / (xC - xA) OrElse (yB - yC) / (xB - xC) = (yC - yA) / (xC - xA) Then<br>                Return -1<br>            End If<br><br>            Dim p1 As Double = Abs(Sqrt((xA - xB) ^ 2 + (yA - yB) ^ 2))<br>            Dim p2 As Double = Abs(Sqrt((xB - xC) ^ 2 + (yB - yC) ^ 2))<br>            Dim p3 As Double = Abs(Sqrt((xC - xA) ^ 2 + (yC - yA) ^ 2))<br><br>            If p1 = 0 OrElse p2 = 0 OrElse p3 = 0 Then<br>                Return -1<br>            End If<br><br>            Dim l1 As Double = (p1 + p2) * 2<br>            Dim l2 As Double = (p2 + p3) * 2<br>            Dim l3 As Double = (p3 + p1) * 2<br><br><br><br><br>            Dim higest As Double = Max(Max(l1, l2), l3)<br>            Dim lowest As Double = Min(Min(l1, l2), l3)<br><br>            Return higest - lowest<br><br>        End Function<br><br><br><br><br>    End Class<br></td>