<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.Arrays;<br> <br>/**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class MysticAndCandies {<br>    public int minBoxes(int C, int X, int[] low, int[] high) {<br>        Arrays.sort(low);<br>        int need = X;<br>        int taken = 0;<br>        for (int i = low.length - 1; i &gt;= 0; --i) {<br>            int x = low[i];<br>            ++taken;<br>            need -= x;<br>            if (need &lt;= 0) break;<br>        }<br>        int res = taken;<br>        taken = low.length;<br>        need = C - X;<br>        Arrays.sort(high);<br>        for (int x : high) {<br>            need -= x;<br>            --taken;<br>            if (need &lt; 0) {<br>                ++taken;<br>                break;<br>            }<br>        }<br>        res = Math.min(res, taken);<br>        return res;<br>    }<br>}<br></td>