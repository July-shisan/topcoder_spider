<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class FlipGame<br> <br>    Private Function minOperations(ByVal board As Boolean(,), ByVal h As Integer, ByVal w As Integer) As Integer<br>        Dim first_path(w - 1) As Integer<br>        Dim bef As Integer<br>        Dim lower_left As Boolean = board(h - 1, 0)<br>        For j As Integer = 0 To w - 1<br>            For i As Integer = h - 1 To 0 Step -1<br>                If board(i, j) &lt;&gt; lower_left Then<br>                    first_path(j) = Math.Max(bef, i + 1)<br>                    bef = first_path(j)<br>                    Exit For<br>                End If<br>                If i = 0 Then first_path(j) = bef<br>            Next<br>        Next<br>        For Each e As Integer In first_path<br>            If e Then<br>                ' perform operation<br>                For j As Integer = 0 To w - 1<br>                    For i As Integer = first_path(j) To h - 1<br>                        board(i, j) = Not board(i, j)<br>                    Next<br>                Next<br>                Return minOperations(board, h, w) + 1<br>            End If<br>        Next<br>        If lower_left Then Return 1<br>    End Function<br> <br>    Public Function minOperations(ByVal board As String()) As Integer<br>        Dim h As Integer = board.Length<br>        Dim w As Integer = board(0).Length<br>        Dim ret(h - 1, w - 1) As Boolean<br>        For i As Integer = 0 To h - 1<br>            For j As Integer = 0 To w - 1<br>                ret(i, j) = board(i)(j) = "1"c<br>            Next<br>        Next<br>        Return minOperations(ret, h, w)<br>    End Function<br> <br>End Class<br></td>