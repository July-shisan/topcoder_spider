<td class="problemText" colspan="8" valign="middle" align="left">
            #line 2 "Superking.cpp"<br><br>#include &lt;string&gt;<br>#include &lt;vector&gt;<br>#include &lt;algorithm&gt;<br>#include &lt;stdio.h&gt;<br>using namespace std;<br><br>typedef vector&lt;int&gt; vi;<br>typedef vector&lt;string&gt; vs;<br>typedef long long ll;<br><br>#define LS &lt;<br>#define Size(x) ((x).size())<br>#define LET(v, val) typeof(val) v = (val)<br>#define CLC(act, val) (*({act; static typeof(val) CLCR = (val); &amp;CLCR;}))<br><br>#define FOR(k,a,b) for(typeof(a) k = (a); k LS (b); k++)<br><br>#define FIRST(k,a,b,cond) CLC(LET(k,a); for(; k LS (b); k++) if(cond) break, k)<br>#define FORALL(k,a,b,cond) (FIRST(k,a,b,cond) == (b))<br>#define EXISTS(k,a,b,cond) (!FORALL(k,a,b,!(cond))<br><br>#define SUM(k,a,b,val) \<br>  CLC(LET(k,a); LET(_res, (typeof(val)) (0)); for(; k LS (b); k++) _res += (val), _res)<br><br>#define MAX(k,a,b,val) \<br>  CLC(LET(k,a); LET(_res, val); for(k++; k LS (b); k++) _res &gt;?= (val), _res)<br><br>vi parsevi(string s) {<br>  s += " ";<br>  vi res;<br>  int temp = 0;<br>  FOR(k,0,Size(s)) if(s[k] == ' ') {res.push_back(temp); temp = 0;} else {temp *= 10; temp += s[k] - '0';}<br>  return res;<br>  }<br> <br>class Superking {<br>  public:<br>  long long squares(int N0, int M0) {<br>    ll N = N0; ll M = M0;<br>    ll v1 = (2*N+1+4*M)*(2*N+1+4*M) - M*(M+1)*8;<br>    ll v2 = M*M + (M+1)*(M+1);<br>    if(N0 &gt; 1) return v1;<br>    if(N0 == 0) return (M+1)*(M+1);<br>    if(N0 == 1) return v1 - v2;<br>    return v1;<br>    }<br>  };<br><br><br>// Powered by FileEdit<br></td>