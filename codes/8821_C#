<td class="problemText" colspan="8" valign="middle" align="left">
            using System;<br>using System.Collections.Generic;<br><br>using System.Text;<br><br><br>    class EvilRobot<br>    {<br>        public string createProgram(int N, int robotX, int robotY, int trapX, int trapY)<br>        {<br>            if (trapX &lt; robotX || trapY &lt; robotY)<br>                return "I'LL GET YOU NEXT TIME";<br>            //int nx = trapX - robotY, ny = trapY - robotY;<br>            int t = trapX + trapY  - robotX - robotY;<br>            if (t &lt;= N)<br>            {<br>                int x = trapX - robotX;<br>                int y = trapY - robotY;<br>                StringBuilder cs = new StringBuilder();<br>                for (int i = 0; i &lt; x; i++)<br>                    cs.Append("R");<br>                for (int j = 0; j &lt; y; j++)<br>                    cs.Append("U");<br>                for (int i = 0; i &lt; N - t; i++)<br>                    cs.Append("R");<br>                return cs.ToString();<br>            }<br>            for (int x = 0; x &lt; N; x++)<br>            {<br>                for (int y = 0; y &lt; N - x; y++)<br>                {<br>                    int temp = trapX + trapY + x + y - robotX - robotY;<br>                    if (temp % N != 0) continue;<br>                    else<br>                    {<br>                        int numx = (trapX + x - robotX )/ (temp / N);<br>                        //if (N == temp)<br>                        //{<br>                        //    StringBuilder cs = new StringBuilder();<br>                        //    for (int i = 0; i &lt; x; i++)<br>                        //        cs.Append("R");<br>                        //    for (int j = 0; j &lt; y; j++)<br>                        //        cs.Append("U");<br>                        //    for (int i = 0; i &lt; N - (x + y); i++)<br>                        //        cs.Append("R");<br>                        //    return cs.ToString();<br><br>                        //}<br>                        int numy=(trapY+y-robotY)/(temp/N);<br>                        if (numy + numx != N) continue;<br>                        StringBuilder s = new StringBuilder();<br>                        StringBuilder s1=new StringBuilder();<br>                        for (int i = 0; i &lt; x; i++)<br>                            s.Append("R");<br>                        for (int j = 0; j &lt; y; j++)<br>                            s.Append("U");<br>                        for (int i = 0; i &lt; numx - x; i++)<br>                            s1.Append("R");<br>                        for (int i = 0; i &lt; numy - y; i++)<br>                            s1.Append("U");<br>                        s1.Append(s);<br>                        return s1.ToString();<br>                    }<br>                }<br>            }<br>            return "I'LL GET YOU NEXT TIME";<br>        }<br>    }<br></td>