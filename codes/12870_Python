<td class="problemText" colspan="8" valign="middle" align="left">
            import math<br>class BigFatInteger2:<br>    def isDivisible(self, A,B,C,D):<br>        def primes(limit):<br>            Ans=[]<br>            C=[True]*(limit+1)<br>            L=list(range(2,limit+1))<br>            for item in L:<br>                if(C[item]):<br>                    i=2<br>                    while(item*i&lt;=limit):<br>                        C[item*i]=False<br>                        i+=1<br>            for item in L:<br>                if(C[item]):<br>                    Ans+=[item]<br>            return Ans<br>        P=primes(31623)<br>        L={}<br>        ind=0<br>        while(ind&lt;len(P)):<br>            if(A==1):<br>                break<br>            if(A%P[ind]==0):<br>                if(P[ind] in L):<br>                    L[P[ind]]+=1<br>                else:<br>                    L[P[ind]]=1<br>                A//=P[ind]<br>                continue<br>            ind+=1<br>        if(A!=1):<br>            L[A]=1<br>        T={}<br>        ind=0<br>        while(ind&lt;len(P)):<br>            if(C==1):<br>                break<br>            if(C%P[ind]==0):<br>                if(P[ind] in T):<br>                    T[P[ind]]+=1<br>                else:<br>                    T[P[ind]]=1<br>                C//=P[ind]<br>                continue<br>            ind+=1<br>        if(C!=1):<br>            T[C]=1<br>        for item in T:<br>            if(item not in L or L[item]*B&lt;T[item]*D):<br>                return "not divisible"<br>        return "divisible"<br></td>