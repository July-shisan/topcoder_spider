<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class Elections<br> <br>Public Function visit(likelihoods As String()) As Integer<br>  dim total as integer, i as long, c1 as integer, c2 as integer, val() as integer, j as integer<br>  <br>  total=likelihoods(0).length<br>  for i=0 to likelihoods.length()-1<br>    redim preserve val(i)<br>    c1=count(likelihoods(i),"1")<br>    c2=count(likelihoods(i),"2")<br>    <br>    val(i)=((c2-c1)/likelihoods(i).length) * 100<br>  next i<br>  visit=0<br>  for i=0 to val.length()-1<br>    for j=0 to  val.length()-1<br>      if val(visit) &lt; val(j) then<br>        visit=j<br>      end if<br>    next j<br>  next i<br>  <br>End Function<br> <br>function count(str as string, pattern as string) as integer<br>  dim i as long<br>  for i=0 to str.length -1<br>       if str.substring(i,1)=pattern then<br>    count=count+1<br>       end if<br>  next i<br>end function<br>End Class<br></td>