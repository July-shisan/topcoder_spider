<td class="problemText" colspan="8" valign="middle" align="left">
            /**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class EllysScrabble {<br>    public String getMin(String letters, int maxDistance) {<br>        int n = letters.length();<br>        char[] res = new char[n];<br>        boolean[] used = new boolean[n];<br>        for (int i = 0; i &lt; n; i++) {<br>            int best = -1;<br>            if (i - maxDistance &gt;= 0 &amp;&amp; !used[i - maxDistance]) {<br>                best = i - maxDistance;<br>            } else {<br>                for (int j = Math.max(0, i - maxDistance); j &lt;= Math.min(i + maxDistance, n - 1); j++) {<br>                    if (used[j]) {<br>                        continue;<br>                    }<br>                    if (best &lt; 0 || letters.charAt(j) &lt; letters.charAt(best)) {<br>                        best = j;<br>                    }<br>                }<br>            }<br>            used[best] = true;<br>            res[i] = letters.charAt(best);<br>        }<br>        return new String(res);<br>    }<br>}<br></td>