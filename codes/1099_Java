<td class="problemText" colspan="8" valign="middle" align="left">
            /*/*///*/*/ Powered by radedit!            /*/*/*/*/*/ ;<br>/*/*//*/*/  import java . util .           /*/*/*/*/*/ ;<br>/*/*//*/*/  import java . lang . reflect . /*/*/*/*/*/ ;<br>public class Regions {<br>   boolean ddd = false ;<br>  long gcd(long a, long b) {<br>    if (b == 0) return a ;<br>    return gcd(b, a % b) ;<br>  }<br>  public int numTaxes(int[] row, int[] col) {<br>    int i, j, k ;<br>    long r = 0 ;<br>    for (i=0; i+1&lt;row.length; i++) {<br>      long dx = Math.abs(row[i+1] - row[i]) ;<br>      long dy = Math.abs(col[i+1] - col[i]) ;<br>      r += dx + dy ;<br>      if (dx &gt; 0 &amp;&amp; dy &gt; 0) {<br>  long d = gcd(dx, dy) ;<br>  dx /= d ;<br>  dy /= d ;<br>  if ((dx &amp; 1) != 0 &amp;&amp; (dy &amp; 1) != 0) {<br>    r -= d ;<br>  }<br>      }<br>    }<br>    if (r &gt; 2000000000) {<br>      return -1 ;<br>    }<br>    return (int)r ;<br>  }<br> <br>  static void pp(Object o) { System.out.println(o) ; }<br>}<br></td>