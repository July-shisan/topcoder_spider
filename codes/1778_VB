<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br> <br>Public Class BombSweeper<br>    Private b()() As Char<br>    Private w, h As Integer<br> <br>    Private Function test(ByVal x As Integer, ByVal y As Integer) As Boolean<br>        If x &gt;= w OrElse x &lt; 0 OrElse y &gt;= h OrElse y &lt; 0 OrElse b(y)(x) = "." Then Return True<br>        Return False<br>    End Function<br> <br>    Private Function test2(ByVal x As Integer, ByVal y As Integer) As Boolean<br>        If test(x, y) AndAlso test(x - 1, y - 1) AndAlso test(x, y - 1) AndAlso test(x + 1, y - 1) AndAlso test(x - 1, y) AndAlso test(x + 1, y) AndAlso test(x - 1, y + 1) AndAlso test(x, y + 1) AndAlso test(x + 1, y + 1) Then Return True<br>        Return False<br>    End Function<br> <br>    Public Function winPercentage(ByVal board As String()) As Double<br>        w = board(0).Length<br>        h = board.Length<br>        ReDim b(board.Length - 1)<br>        Dim a As Integer<br>        For a = 0 To board.Length - 1<br>            b(a) = board(a).ToCharArray<br>        Next<br>        Dim x, y As Integer<br>        Dim win, lose As Integer<br>        For x = 0 To w - 1<br>            For y = 0 To h - 1<br>                If test2(x, y) Then win += 1<br>                If b(y)(x) = "B" Then lose += 1<br>            Next<br>        Next<br>        Return win / (win + lose) * 100<br>    End Function<br>End Class<br></td>