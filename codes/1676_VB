<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br> <br>Public Class StampPads<br>    Public Function bestCombo(ByVal pads As String(), ByVal wishlist As String()) As Integer<br>        Dim a, b, c As Integer<br>        ReDim have(pads.Length - 1, wishlist.Length - 1)<br>        L = wishlist.Length<br>        P = pads.Length<br>        For a = 0 To pads.Length - 1<br>            Dim list() As String = pads(a).Split(" "c)<br>            For b = 0 To 4<br>                For c = 0 To wishlist.Length - 1<br>                    If wishlist(c) = list(b) Then<br>                        have(a, c) = True<br>                        Exit For<br>                    End If<br>                Next<br>            Next<br>        Next<br>        best = Integer.MaxValue<br>        ReDim covered(L - 1)<br>        doWork(0)<br>        If best &lt; Integer.MaxValue Then<br>            Return best<br>        Else<br>            Return -1<br>        End If<br>    End Function<br> <br>    Dim have(,) As Boolean<br>    Dim best As Integer<br>    Dim covered() As Integer<br>    Dim cov As Integer<br>    Dim used As Integer<br>    Dim L, P As Integer<br> <br>    Private Sub doWork(ByVal index As Integer)<br>        If index = P OrElse L - cov &gt; (P - index) * 5 Then Exit Sub<br>        doWork(index + 1)<br>        Dim a As Integer<br>        used += 1<br>        For a = 0 To L - 1<br>            If have(index, a) Then<br>                If covered(a) = 0 Then cov += 1<br>                covered(a) += 1<br>            End If<br>        Next<br>        If cov = L AndAlso used &lt; best Then<br>            best = used<br>        Else<br>            doWork(index + 1)<br>        End If<br>        used -= 1<br>        For a = 0 To L - 1<br>            If have(index, a) Then<br>                covered(a) -= 1<br>                If covered(a) = 0 Then cov -= 1<br>            End If<br>        Next<br>    End Sub<br>End Class<br></td>