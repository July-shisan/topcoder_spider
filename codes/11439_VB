<td class="problemText" colspan="8" valign="middle" align="left">
            Class CandyShop<br>  Public Function countProbablePlaces(X As Integer(), Y As Integer(), R As Integer()) As Integer<br>    Dim cx as integer, cy as integer<br>      cx = 250<br>      cy = 250<br><br>    Dim field(cx+202, cy + 202) As Integer<br>    Dim N as Integer<br>    N = UBound(X)<br>    Dim i as integer, j as integer<br>    <br>    For i=0 to cx+200: For j=0 to cy+200<br>      field(i,j) = 0<br>    Next j: Next i<br>    <br>    For idx As Integer = 0 To N<br><br>      for i = -200 to 200<br>        for j = -200 to 200<br>          if Math.Abs(X(idx)-i) + Math.Abs(Y(idx)-j) &lt;= R(idx) Then field(cx+i,cy+j) = field(cx+i,cy+j) + 1<br>        next j<br>      next i<br>      <br>    Next idx<br><br>    Dim cnt as integer = 0<br><br>    <br>      for i = -200 to 200<br>        for j = -200 to 200<br>          if field(cx+i, cy+j) = N+1 Then cnt = cnt+1<br>        next j    <br>      next i<br>      <br>    Return cnt<br>  End Function<br>End Class<br></td>