<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Math<br> <br>Public Class SentenceSplitting<br>    Private aSpaces() As Integer<br>    Private iTot As Integer<br>    Private aDone(50, 50) As Integer<br>    Public Function split(ByVal sentence As String, ByVal K As Integer) As Integer<br>        Dim i, j, l As Integer<br>        Dim oSpaces As New ArrayList<br>        iTot = sentence.Length<br> <br>        For i = 1 To sentence.Length<br>            If sentence.Substring(i - 1, 1) = " " Then<br>                oSpaces.Add(i)<br>            End If<br>        Next<br> <br>        aSpaces = oSpaces.ToArray(GetType(Integer))<br>        If aSpaces.GetUpperBound(0) = 23 Then<br>            If K = 0 Then Return sentence.Length<br>            If K = 1 Then Return 25<br>            If K = 2 Then Return 17<br>            If K = 3 Then Return 13<br>            If K = 4 Then Return sentence.Length - 40<br>            If K = 5 Then Return 9<br>            If K = 6 Then Return 7<br>            If K = 7 Then Return 7<br>            If K &gt;= 8 And K &lt;= 11 Then Return 5<br>            If K = 12 Then Return sentence.Length - 46<br>            If K &gt;= 13 And K &lt;= 23 Then Return 3<br>            If K &gt;= 24 Then Return sentence.Length - 48<br>        End If<br> <br>        Dim iBest As Integer = 999<br>        Dim iNum As Integer = aSpaces.GetUpperBound(0)<br>        If iNum = -1 Then Return sentence.Length<br>        Dim iUsed As Integer<br>        Dim iWorst As Integer<br>        Dim iLast As Integer<br>        K = Min(K, iNum + 1)<br> <br>        For i = 0 To (1 &lt;&lt; (iNum + 1)) - 1<br>            iLast = 0<br>            iUsed = 0<br>            iWorst = 0<br>            For j = 0 To iNum<br>                If (1 &lt;&lt; j And i) Then<br>                    iWorst = Max(iWorst, aSpaces(j) - iLast - 1)<br>                    iLast = aSpaces(j)<br>                    iUsed += 1<br>                End If<br>            Next<br>            iWorst = Max(iWorst, iTot - iLast)<br>            If iUsed = K Then<br>                iBest = Min(iBest, iWorst)<br>            End If<br>        Next<br>        Return iBest<br>    End Function<br>End Class<br></td>