<td class="problemText" colspan="8" valign="middle" align="left">
            #line 2 "IndicatorMotionReverse.cc"<br>#include &lt;string&gt;<br>#include &lt;vector&gt;<br>#include &lt;stdio.h&gt;<br>#include &lt;math.h&gt;<br>#include &lt;string.h&gt;<br>#include &lt;ctype.h&gt;<br>#include &lt;algorithm&gt;<br>#include &lt;stdlib.h&gt;<br>using namespace std;<br> <br>class IndicatorMotionReverse {<br>public:<br>    string getMinProgram(vector &lt;string&gt; actions) {<br>  string input;<br>  vector&lt;int&gt; in;<br>  for (int i = 0; i &lt; actions.size(); i++)<br>      input = input + actions[i];<br>  for (int i = 0; i &lt; input.size(); i++)<br>      switch (input[i]) {<br>      case '-':<br>    in.push_back(0);<br>    break;<br>      case '/':<br>    in.push_back(1);<br>    break;<br>      case '|':<br>    in.push_back(2);<br>    break;<br>      case 'N':<br>    in.push_back(3);<br>    break;<br>      }<br>  string output;<br>  int p = 0;<br>  while (p + 1 &lt; in.size()) {<br>      int d = (4 + in[p+1] - in[p]) % 4;<br>      int q = p;<br>      while (q + 1 &lt; in.size() &amp;&amp; d == (4 + in[q+1] - in[q]) % 4)<br>    q++;<br>      char c = "SLFR"[d];<br>      char buf[8];<br>      sprintf(buf, "%c%02d", c, 1 + (q - p - 1) % 99);<br>      output += buf;<br>      for (int i = 0; i &lt; (q - p - 1) / 99; i++) {<br>    sprintf(buf, "%c99", c);<br>    output += buf;<br>      }<br>      p = q;<br>  }<br>  if (output.size() &gt; 100) {<br>      output.resize(97);<br>      output += "...";<br>  }<br>  return output;<br>    }<br>};<br> <br> <br> <br>// Powered by FileEdit<br></td>