<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Diagnostics<br> <br>Public Class BenfordsLaw<br>    Public Function questionableDigit(ByVal transactions As Integer(), ByVal threshold As Integer) As Integer<br>        Dim count(9) As Integer<br>        Dim a As Integer<br>        For a = 0 To transactions.Length - 1<br>            count(Asc(transactions(a).ToString.Chars(0)) - Asc("1") + 1) += 1<br>        Next<br>        If transactions.Length = 0 Then Return -1<br>        For a = 1 To 9<br>            Dim expected As Double = Log(1 + 1 / a, 10) * transactions.Length<br>            If count(a) / expected &gt;= threshold OrElse count(a) / expected &lt;= 1 / threshold Then Return a<br>        Next<br>        Return -1<br>    End Function<br>End Class<br></td>