<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System<br>Imports Microsoft.VisualBasic<br> <br>Public Class SkipRope<br>  Public Function partners(candidates As Integer(), height As Integer) As Integer()<br>    Dim tempDiff() As String<br>    Dim i As Integer, j As Integer<br> <br>    ReDim partners(1)<br>    ReDim tempDiff(candidates.Length - 1)<br>    <br>    For i = 0 To candidates.Length - 1<br>      tempDiff(i) =   Format(Math.Abs(candidates(i) - height), "00000") &amp; Format(100000 - candidates(i), "00000")<br>    Next<br>    Array.Sort(tempDiff, candidates)<br>    partners.SetValue(candidates(0), 0)<br>    partners.SetValue(candidates(1), 1)<br>    Array.Sort(partners)<br>  End Function<br>  <br> <br>End Class<br></td>