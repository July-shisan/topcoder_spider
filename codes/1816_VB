<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br> <br>Public Class Logger<br>    Public Function log(ByVal messages As String(), ByVal priorities As String(), ByVal precedence As String(), ByVal loggingPriority As String) As String()<br>        Dim l As New ArrayList<br>        Dim a As Integer<br>        Dim h As New Hashtable<br>        For a = 0 To precedence.Length - 1<br>            h.Add(precedence(a).ToLower, a)<br>        Next<br>        For a = 0 To messages.Length - 1<br>            If test(h, priorities(a).ToLower, loggingPriority.ToLower) Then<br>                l.Add(messages(a))<br>            End If<br>        Next<br>        Return l.ToArray(GetType(String))<br>    End Function<br> <br>    Private Function test(ByVal h As Hashtable, ByVal pri As String, ByVal log As String) As Boolean<br>        Dim p1 As String<br>        Dim pn As Integer<br>        Dim l1 As String<br>        Dim ln As Integer<br>        If pri.IndexOf(" "c) &gt; -1 Then<br>            Dim s() As String = pri.Split(" "c)<br>            p1 = s(0)<br>            pn = s(1)<br>        Else<br>            p1 = pri<br>            pn = 0<br>        End If<br>        If log.IndexOf(" "c) &gt; -1 Then<br>            Dim s() As String = log.Split(" "c)<br>            l1 = s(0)<br>            ln = s(1)<br>        Else<br>            l1 = log<br>            ln = 0<br>        End If<br>        Return h(p1) &gt; h(l1) OrElse (h(p1) = h(l1) AndAlso pn &gt;= ln)<br>    End Function<br>End Class<br></td>