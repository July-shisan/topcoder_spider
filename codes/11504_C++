<td class="problemText" colspan="8" valign="middle" align="left">
            // unfortunately I have no ideas for a good line here<br> <br>#include &lt;algorithm&gt;<br>#include &lt;string&gt;<br>#include &lt;map&gt;<br>using namespace std;<br> <br>#define Size(x) (int(x.size()))<br> <br>#line 3 "SistersErasingLetters.cpp"<br> <br>map&lt;string, string&gt; memo;<br> <br>string worst(string word);<br> <br>string best(string word) {<br>  if(memo.count(word)) return memo[word];<br>  if(word == "") return "";<br>  string ret = "";<br>  for(int k=0; k&lt;Size(word); k++) ret = max(ret, word.substr(0, k) + worst(word.substr(k+1)));<br>  return memo[word] = ret;<br>  }<br> <br>string worst(string word) {<br>  if(word == "") return "";<br>  string ret = "~";<br>  for(int k=0; k&lt;Size(word); k++) ret = min(ret, word.substr(0, k) + best(word.substr(k+1)));<br>  return ret;<br>  }<br> <br>class SistersErasingLetters {<br>  public:<br>  string whoWins(string word) {<br>    // !FDI<br>    return best(word) &gt; word ? "Camomile" : "Romashka";<br>    }<br>  };<br> <br> <br> <br> <br>// Powered by FileEdit<br>// Powered by TZTester 1.01 [25-Feb-2003]<br>// Powered by CodeProcessor<br></td>