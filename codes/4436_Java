<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.*;<br> <br>public class PerforatedSheet<br>{<br>    public double[] getCenterOfMass(int sheetWidth, int sheetHeight, int[] x, int[] y, int[] width, int[] height)<br>    {<br>        long mass = (long)sheetWidth * (long)sheetHeight;<br>        long sumX = (long)mass * (long)sheetWidth;<br>        long sumY = (long)mass * (long)sheetHeight;<br>        for (int i=0; i &lt; x.length; i++)<br>        {<br>            long m = (long)width[i] * (long)height[i];<br>            mass -= m;<br>            sumX -= m * (2 *(long)x[i] + (long)width[i]);<br>            sumY -= m * (2 *(long)y[i] + (long)height[i]);<br>        }<br>        if (mass == 0) return new double[]{};<br>        return new double[] { 0.5 * sumX / (double)mass, 0.5 * sumY / (double)mass};<br>    }<br> <br>}<br></td>