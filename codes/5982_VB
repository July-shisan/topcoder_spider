<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class SpeedTyper<br>  Public Function lettersToPractice(ByVal letters As String, ByVal times As Integer()) As String<br>    Dim avg as Integer = 0<br>    Dim tot as Integer = 0<br>    Dim res as String = ""<br>    <br>    avg = times(times.length-1) / times.length<br>    if times(0) &gt; avg then res = letters.substring(0,1)<br>    for i as integer = 1 to times.length - 1<br>      if times(i) - times(i-1) &gt; avg then<br>        res += letters.substring(i,1)<br>      end if<br>    next i<br>    <br>    return res<br>  End Function<br>End Class<br></td>