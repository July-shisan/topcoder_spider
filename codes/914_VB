<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Public Class BankingArray<br>    Public Function addCost(ByVal initialCapacity As Integer, ByVal numAdds As Integer) As Integer<br>        If numAdds &lt;= initialCapacity Then<br>            Return numAdds<br>        Else<br>            Dim a As Long, p As Long, i As Long<br>            a = Floor(Log(numAdds / initialCapacity) / Log(2))<br>            If Floor(Log(numAdds / initialCapacity) / Log(2)) &lt;&gt; Log(numAdds / initialCapacity) / Log(2) Then a += 1<br>            p = initialCapacity<br>            For i = 1 To a<br>                p += (initialCapacity * Pow(2, i))<br>            Next<br>            p += numAdds - (initialCapacity * Pow(2, a))<br>            return p<br>        End If<br>    End Function<br>End Class<br></td>