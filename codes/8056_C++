<td class="problemText" colspan="8" valign="middle" align="left">
            #include&lt;string&gt;<br>#include&lt;vector&gt;<br>#include&lt;math.h&gt;<br>#include&lt;sstream&gt;<br>using namespace std;<br>class SignificanceArithmetic{<br>  public:<br>  string evaluate(string expression);<br>};<br> <br>string SignificanceArithmetic::evaluate(string expression){<br>  int len1=0,len2=0;<br>  bool point=false;<br>  int i=0;<br>  long long val1=0,val2=0;<br>  for(;expression[i]!='+';i++)<br>    if(expression[i]=='.')<br>      point=true;<br>    else{<br>      val1=val1*10+(expression[i]-'0');<br>      if(point)len1++;<br>    }<br>  point=false;<br>  for(i++;i&lt;expression.length();i++)<br>    if(expression[i]=='.')<br>      point=true;<br>    else{<br>      val2=val2*10+(expression[i]-'0');<br>      if(point)len2++;<br>    }<br>  int alen=abs(len1-len2);<br>  while(len1&lt;len2){<br>    val1*=10;<br>    len1++;<br>  }<br>  while(len1&gt;len2){<br>    val2*=10;<br>    len2++;<br>  }<br>  int shax=1;<br>  len1-=alen;<br>  while(alen){<br>    shax*=10;<br>    alen--;<br>  }<br>  val1+=val2;<br>  if(val1%shax&gt;shax/2)<br>    val1+=shax;<br>  else if(shax&gt;1&amp;&amp;val1%shax==shax/2&amp;&amp;((val1/shax)&amp;1))<br>    val1+=shax;<br>  val1/=shax;<br>  shax=1;<br>  while(len1){<br>    shax*=10;<br>    len1--;<br>  }<br>  ostringstream res;<br>  res&lt;&lt;val1/shax;<br>  if(shax!=1){<br>    res&lt;&lt;'.';<br>    for(shax/=10;shax;shax/=10)<br>      res&lt;&lt;((val1/shax)%10);<br>  }<br>  return res.str();<br>}<br></td>