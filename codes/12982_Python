<td class="problemText" colspan="8" valign="middle" align="left">
            class MinimumSquareEasy(object):<br>    def minArea(self, x, y):<br>        n = len(x)<br>        cands = []<br> <br>        for i in xrange(n - 1):<br>            for j in xrange(i + 1, n):<br>                cx = x[:i] + x[i + 1:j] + x[j + 1:]<br>                cy = y[:i] + y[i + 1:j] + y[j + 1:]<br> <br>                min_x, max_x = min(cx) - 1, max(cx) + 1<br>                min_y, max_y = min(cy) - 1, max(cy) + 1<br> <br>                if max_x - min_x &gt; max_y - min_y:<br>                    cands.append(max_x - min_x)<br>                else:<br>                    cands.append(max_y - min_y)<br> <br>        return min(cands) ** 2<br></td>