<td class="problemText" colspan="8" valign="middle" align="left">
            Class CityMap<br><br>Public Function getLegend(map() as String, POIs() as Integer) as String<br>  dim S(POIs.getupperbound(0)) as string<br>  dim S1(asc("Z")) as string<br>  dim i as integer = 0<br>  for i=asc("A") to asc("Z")<br>    S1(i)=0<br>  next<br>  for i=0 to map.getupperbound(0)<br>    dim j as integer=0<br>    for j=0 to map(i).length-1<br>      S1(asc(map(i).substring(j,1))) = S1(asc(map(i).substring(j,1))) + 1<br>    next<br>  next<br>  dim line as string=""<br>  for i=0 to POIs.getupperbound(0)<br>    dim j as integer=0<br>    for j=asc("A") to asc("Z")<br>      if S1(j)=POIs(i) then line &amp;= chr(j)<br>    next<br>  next<br>  return (line)<br>End Function <br><br><br>End Class<br></td>