<td class="problemText" colspan="8" valign="middle" align="left">
            imports microsoft.visualbasic<br>imports system<br>imports system.diagnostics.debug<br>public class ShoelaceLength<br>public function calculate(startWidth as integer, endWidth as integer, spread as integer, numPairs as integer) as double<br>  dim r, dist, tmp, width, delta, m as double<br>  dim i as integer<br>  r = startWidth<br>  width = startWidth<br>  dist = (numPairs - 1) * spread<br>  delta = (endWidth - startWidth) / (numPairs - 1)<br>  for i = 1 to (numPairs - 1)<br>    m = width + delta/2<br>    tmp = system.math.sqrt((m * m) + (spread * spread))<br>    r = r + (tmp * 2)<br>    width = width + delta<br>  next i<br> <br>  return r<br>end function<br> <br>end class<br></td>