<td class="problemText" colspan="8" valign="middle" align="left">
            public class Barbecue<br>  Public Function eliminate(n As Integer, voter As Integer(), excluded As Integer()) As Integer<br>    Dim votesReceived(n - 1) as integer<br>    Dim votesCast(n - 1) as integer<br> <br>    Dim i as integer<br> <br>    Dim loser as integer<br>    Dim loserVotes as integer<br> <br>    for i = 0 to n - 1<br>      votesReceived(i) = 0<br>      votesCast(i) = 0<br>    next<br> <br>    for i = 0 to voter.GetUpperBound(0)<br>      votesReceived(excluded(i)) = votesReceived(excluded(i)) + 1<br>      votesCast(voter(i)) = votesCast(voter(i)) + 1<br>    next<br>  <br>    loser = 0<br>    loserVotes = 0<br> <br>    for i = 0 to n - 1<br>      if votesReceived(i) &gt; loserVotes then<br>        loser = i<br>        loserVotes = votesReceived(i)<br>      elseif votesReceived(i) = loserVotes<br>        if votesCast(i) &gt; votesCast(loser) then<br>          loser = i<br>          loserVotes = votesReceived(i)<br>        end if<br>      end if<br>    next<br>  <br>    return loser<br>    <br>  end function<br>end class<br></td>