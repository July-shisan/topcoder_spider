<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System.Collections.Generic<br>Imports System.Array<br>Imports System.Math<br> <br>Public Class TheDevice<br>    Public Function minimumAdditional(ByVal plates As String()) As Integer<br>        ' to distinguish or, xor and and, testing "0 op 1" and "1 op 1" is necessary and sufficient.<br>        ' at least two 1's and one 0 is needed.<br>        Dim zero, one As Integer<br>        For i As Integer = 0 To plates(0).Length - 1<br>            zero = 1<br>            one = 2<br>            For Each plate As String In plates<br>                If plate(i) = "0"c Then zero -= 1<br>                If plate(i) = "1"c Then one -= 1<br>            Next<br>            minimumAdditional = Max(minimumAdditional, Max(zero, 0) + Max(one, 0))<br>        Next<br>    End Function<br> <br>End Class<br></td>