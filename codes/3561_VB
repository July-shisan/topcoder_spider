<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class SortEstimate<br>        Public Function howMany(ByVal c As Integer, ByVal time As Integer) As Double<br>            Dim dMin As Double<br>            Dim dMax As Double<br>            Dim dChange As Double = 1<br>            Dim dNew As Double<br>            Dim dTime As Double<br>            Dim dOld As Double<br> <br>            dMin = 0<br>            dMax = -1<br>            dNew = 1<br> <br>            Do While dChange &gt; 0.00000000000001<br>                dOld = dNew<br>                dTime = dHowLong(c, dNew)<br>                If dTime &lt; time Then<br>                    If dMax = -1 Then<br>                        dMin = dNew<br>                        dNew *= 2<br>                    Else<br>                        dMin = dNew<br>                        dNew = (dMin + dMax) / 2<br>                    End If<br>                Else<br>                    dMax = dNew<br>                    dNew = (dMin + dMax) / 2<br>                End If<br>                dChange = System.Math.Abs(dNew - dOld)<br>            Loop<br> <br>            Return dNew<br>        End Function<br> <br>        Private Function dHowLong(ByVal c As Integer, ByVal n As Double) As Double<br>            Return CDbl(c) * n * System.Math.Log(n) / System.Math.Log(2)<br>        End Function<br>    End Class<br></td>