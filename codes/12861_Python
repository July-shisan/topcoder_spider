<td class="problemText" colspan="8" valign="middle" align="left">
            class BinPacking:<br>    def minBins(self, item):<br>        n = 0<br>        item = list(item)<br>        item.sort()<br>        while len(item) &gt; 0:<br>            w = item.pop()<br>            h = None<br>            for i in range(len(item)-1, -1, -1):<br>                if item[i] + w &lt;= 300:<br>                    h = i<br>                    break<br>            if None != h:<br>                w += item[h]<br>                del item[h]<br>            h = None<br>            for i in range(len(item)-1, -1, -1):<br>                if item[i] + w &lt;= 300:<br>                    h = i<br>                    break<br>            if None != h:<br>                w += item[h]<br>                del item[h]<br>            n += 1<br>        return n<br></td>