<td class="problemText" colspan="8" valign="middle" align="left">
            imports Microsoft.VisualBasic<br>imports System<br> <br>Public Class SandwichBar<br>  Public Function whichOrder(ByVal available as string(), ByVal orders as string()) as integer<br>    dim iResult = -1<br>    dim iOrder as integer = 0<br>    dim sCheck as string<br>    dim sDesire as string = ""<br>    for iOrder = 0 to orders.GetUpperBound(0)<br>      dim bNotFound as boolean = false<br>      dim sIngred as string()  = orders(iOrder).split(" ")<br>      for each sDesire in sIngred<br>        if IsIngreAvailable(sDesire, available) = false then bNotFound = true<br>        if bNotFound = true then exit for<br>      next sDesire<br>      if bNotFound = false then<br>        iResult = iOrder<br>        Exit For<br>      end if<br>    next iOrder<br>    return iResult<br>  End Function<br>  <br>  Private Function IsIngreAvailable(sIng as string, Available as string()) as boolean<br>    dim bResult as boolean = false<br>    dim sCheck as string<br>    for each sCheck in Available<br>      if sCheck = sIng then bResult = true<br>    next sCheck<br>    return bResult<br>  End Function<br>End Class<br></td>