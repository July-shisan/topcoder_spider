<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.*;<br> <br>public class TheLotteryBothDivs {<br>  public double find(String[] goodSuffixes) {<br>        Set&lt;String&gt; suffixes = new HashSet&lt;String&gt;();<br>        for (String x : goodSuffixes) suffixes.add(x);<br>        outer:<br>        while (true) {<br>            for (String x : suffixes)<br>                for (String y : suffixes)<br>                    if (x.endsWith(y) &amp;&amp; !x.equals(y)) {<br>                        suffixes.remove(x);<br>                        continue outer;<br>                    }<br>            break;<br>        }<br>    double res = 0;<br>        for (String x : suffixes)<br>            res += Math.pow(0.1, x.length());<br>    return res;<br>  }<br> <br>}<br> <br> <br>// Powered by FileEdit<br>// Powered by RETester [based on ExampleBuilder]<br>// Powered by CodeProcessor<br></td>