<td class="problemText" colspan="8" valign="middle" align="left">
            class ColorfulRoad:<br>    def getMin(self, road):<br>        MAX = 9999999<br>        a = [MAX] * len(road)<br>        a[0] = 0<br>        for i in range(0, len(road)-1):<br>            if a[i] &gt;= 0:<br>                for j in range(i+1, len(road)):<br>                    if self.judgeValid(road[i], road[j]):<br>                        cost = a[i] + (j-i) * (j-i)<br>                        a[j] = min(a[j], cost)<br>                        <br>        if a[len(road)-1] == MAX:<br>            return -1<br>        else:<br>            return a[len(road)-1]<br>    <br>    def judgeValid(self, fromch, toch):<br>        step = ['R', 'G', 'B']<br>        i = step.index(fromch)<br>        return toch == step[(1+i)%3]<br></td>