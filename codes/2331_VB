<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class PolicePair<br>    Private Structure Pair<br>        Implements IComparable<br> <br>        Dim Min As Integer<br>        Dim Max As Integer<br> <br>        Public Function CompareTo(ByVal obj As Object) As Integer Implements System.IComparable.CompareTo<br>            Dim it As Pair = obj<br>            If Me.Min &lt;&gt; it.Min Then<br>                Return Me.Min - it.Min<br>            Else<br>                Return Me.Max - it.Max<br>            End If<br>        End Function<br>    End Structure<br> <br>    Public Function bestSquad(ByVal skillStart As Integer(), ByVal skillEnd As Integer()) As Integer()<br>        'Dim list(skillStart.Length - 1) As Pair<br>        'Dim a As Integer<br>        'For a = 0 To skillStart.Length - 1<br>        '    list(a).Min = skillStart(a)<br>        '    list(a).Max = skillEnd(a)<br>        'Next<br>        'Array.Sort(list)<br>        Dim count(1000) As Integer<br>        Dim all As Integer<br>        Dim a, b As Integer<br>        For a = 0 To skillStart.Length - 1<br>            For b = skillStart(a) To skillEnd(a)<br>                count(b) += 1<br>            Next<br>            all += skillEnd(a) - skillStart(a) + 1<br>        Next<br>        Dim c, d As Integer<br>        Dim best As Integer<br>        Dim bestSkill As Integer<br>        'center<br>        For c = 1 To 1000<br>            Dim cnt As Integer = 0<br>            cnt += count(c) \ 2<br>            For d = 1 To Min(c - 1, 1000 - c)<br>                cnt += Min(count(c - d), count(c + d))<br>            Next<br>            If cnt &gt; best OrElse (cnt = best AndAlso c &gt; bestSkill) Then<br>                best = cnt<br>                bestSkill = c<br>            End If<br>        Next<br>        'offcenter<br>        For c = 1 To 999<br>            Dim cnt As Integer = 0<br>            For d = 1 To Min(c, 1000 - c)<br>                cnt += Min(count(c - d + 1), count(c + d))<br>            Next<br>            If cnt &gt; best OrElse (cnt = best AndAlso c + 0.5D &gt; bestSkill) Then<br>                best = cnt<br>                bestSkill = c<br>            End If<br>        Next<br>        Return New Integer() {all - 2 * best, bestSkill}<br>    End Function<br>End Class<br></td>