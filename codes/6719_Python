<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class UsingStats<br><br>    Public Function getMedian(ByVal vals As Integer(), ByVal mean As Integer) As Integer<br>        Dim i As Integer<br>        Dim sum As Integer = mean * (vals.Length + 1)<br>        For i = 0 To vals.Length - 1<br>            sum = sum - vals(i)<br>        Next<br>        Dim missing As Integer = sum<br><br>        Dim fullset() As Integer<br>        ReDim fullset(vals.Length)<br>        For i = 0 To vals.Length - 1<br>            fullset(i) = vals(i)<br>        Next<br>        fullset(vals.Length) = missing<br><br>        fullset.Sort(fullset)<br>        Return fullset(vals.Length / 2)<br>    End Function<br>End Class<br></td>