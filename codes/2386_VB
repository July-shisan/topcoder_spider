<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class Percents<br>    Public Function minSamples(ByVal percent As String) As Integer<br>        Dim p As Decimal = percent.Substring(0, 5)<br>        Dim i As Integer = p * 100<br>        Dim a As Integer<br>        For a = 1 To 10000<br>            Dim d As Integer = Floor(a * p / 100)<br>            If Int(CDec(d) / CDec(a) * 10000 + 0.5D) = i Then Return a<br>            d = Ceiling(a * p / 100)<br>            If Int(CDec(d) / CDec(a) * 10000 + 0.5D) = i Then Return a<br>        Next<br>    End Function<br>End Class<br></td>