<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Math<br> <br>Public Class CountriesRanklist<br>    Public Function findPlaces(ByVal knownPoints As String()) As String()<br>        Dim i, j, k, l As Long<br> <br>        Dim aCountries As New SortedList(New IMyComp)<br>        Dim s() As String<br> <br>        For i = 0 To knownPoints.GetUpperBound(0)<br>            s = knownPoints(i).Split(" ")<br>            If Not aCountries.ContainsKey(s(0)) Then<br>                aCountries.Add(s(0), Nothing)<br>            End If<br>        Next<br> <br>        Dim aMax(aCountries.Count - 1) As Integer<br>        Dim aMin(aCountries.Count - 1) As Integer<br>        Dim aTot(aCountries.Count - 1) As Integer<br>        Dim aMissing(aCountries.Count - 1) As Integer<br> <br>        For i = 0 To aMax.GetUpperBound(0)<br>            aMissing(i) = 4<br>            aTot(i) = 0<br>        Next<br> <br>        k = 999<br> <br>        For i = 0 To knownPoints.GetUpperBound(0)<br>            s = knownPoints(i).Split(" ")<br>            j = aCountries.IndexOfKey(s(0))<br>            aMissing(j) -= 1<br>            aTot(j) += CInt(s(2))<br>            k = Min(k, CInt(s(2)))<br>        Next<br> <br>        If k = 0 Then k = 1<br> <br>        Dim aBest(aMin.GetUpperBound(0))<br>        Dim aWorst(aMin.GetUpperBound(0))<br> <br>        For i = 0 To aMax.GetUpperBound(0)<br>            aMax(i) = aTot(i) + aMissing(i) * (k - 1)<br>            aBest(i) = 1<br>            aWorst(i) = aCountries.Count<br>            For j = 0 To aMax.GetUpperBound(0)<br>                If i &lt;&gt; j Then<br>                    If aTot(j) &gt; aMax(i) Then aBest(i) += 1<br>                    If aTot(j) + aMissing(j) * (k - 1) &lt;= aTot(i) Then aWorst(i) -= 1<br>                End If<br>            Next<br>        Next<br> <br>        Dim sRet(aMax.GetUpperBound(0)) As String<br> <br>        For i = 0 To aMax.GetUpperBound(0)<br>            sRet(i) = aCountries.GetKey(i) &amp; " " &amp; aBest(i) &amp; " " &amp; aWorst(i)<br>        Next<br> <br>        Return sRet<br>    End Function<br> <br>    Private Class IMyComp<br>        Implements IComparer<br> <br>        Public Function Compare(ByVal x As Object, ByVal y As Object) As Integer Implements System.Collections.IComparer.Compare<br>            If x &gt; y Then Return 1<br>            If y &gt; x Then Return -1<br>            If x = y Then Return 0<br>        End Function<br>    End Class<br>End Class<br></td>