<td class="problemText" colspan="8" valign="middle" align="left">
            // Author:  King Larry Mak<br>// TC Handle:  Larry<br>import java.util.*;<br>public class VolumeGuess {<br>  public int correctVolume(String[] queries, int numberOfBoxes, int ithBox) {<br>    int[] box = new int[numberOfBoxes];<br>    for ( int i = 0; i &lt; queries.length; i++ ) {<br>      StringTokenizer st = new StringTokenizer( queries[i], "," );<br>      int a = Integer.parseInt( st.nextToken() );<br>      int b = Integer.parseInt( st.nextToken() );<br>      int c = Integer.parseInt( st.nextToken() );<br>      box[a-1] = Math.max( box[a-1], c );<br>      box[b-1] = Math.max( box[b-1], c );<br>    }<br>    return box[ithBox-1];<br>  }<br>  <br>}<br> <br>// Powered by PopsEdit<br>// Powered by CodeProcessor<br></td>