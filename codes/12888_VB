<td class="problemText" colspan="8" valign="middle" align="left">
            Imports System.Collections.Generic<br> <br>Public Class ORSolitaire<br>    Public Function getMinimum(ByVal validMoves As Integer(), ByVal goal As Integer) As Integer<br>        Dim moves = New List(Of Integer)(validMoves)<br>        For Each move In validMoves<br>            If (goal Or move) &lt;&gt; goal Then moves.Remove(move) ' move has extra bit(s)<br>        Next<br>        getMinimum = moves.Count<br>        For i = 0 To 31<br>            Dim p = 1 &lt;&lt; i<br>            If (goal And p) = 0 Then Continue For<br>            Dim c = 0<br>            For Each move In moves<br>                If (move And p) Then c += 1<br>            Next<br>            getMinimum = Math.Min(c, getMinimum)<br>        Next<br>    End Function<br> <br>End Class<br></td>