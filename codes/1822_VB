<td class="problemText" colspan="8" valign="middle" align="left">
            imports microsoft.visualbasic.strings<br>public class LetterFreq<br>Public Function getFreqs(doc As String()) As Integer()<br>  dim letters(25) as integer<br>  dim i,j,k as integer<br>  for i = 0 to doc.length - 1<br>    doc(i) = UCase(doc(i))<br>    for j = 1 to doc(i).length<br>      if (getChar(doc(i),j) &gt;= "A" and getChar(doc(i),j) &lt;= "Z") then<br>        k = Asc(GetChar(doc(i),j)) - Asc("A")<br>        letters(k) = letters(k) + 1<br>      end if<br>    next j<br>  next i<br>  getFreqs = letters<br>end function<br>end class<br></td>