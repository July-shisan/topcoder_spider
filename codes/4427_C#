<td class="problemText" colspan="8" valign="middle" align="left">
            using System;<br> <br>public class StepperMotor {<br>  public int rotateToNearest(int n, int current, int[] target) {<br>    long ans = 0, best = 3000000000;<br> <br>    long a = n;<br>    while (a &lt; 3000000000) a &lt;&lt;= 1;<br> <br>    long c = current;<br>    c += a;<br>    c %= n;<br> <br>    for (int i = 0; i &lt; target.Length; i++) {<br>      long x = target[i];<br>      x += a;<br>      x %= n;<br> <br>      while (x &gt; c) x -= n;<br> <br>      if (c - x &lt; best) {<br>        best = c - x;<br>        ans = -best;<br>      }<br> <br>      while (x &lt; c) x += n;<br> <br>      if (x - c &lt;= best) {<br>        best = ans = x - c;<br>      }<br>    }<br> <br>    return (int) ans;<br>  }<br>}<br></td>