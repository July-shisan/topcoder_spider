<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System.Math<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class FullMonitor<br>    Private Structure pixel<br>        Dim x As Integer<br>        Dim y As Integer<br>        Dim z As Integer<br>    End Structure<br>    Private Structure pair<br>        Dim x1 As Integer<br>        Dim x2 As Integer<br>    End Structure<br>    Public Function numLasers(ByVal pixels As String()) As Integer<br>        Dim a, b, c As Integer<br>        Dim cc() As String<br>        Dim p(pixels.Length - 1) As pixel<br>        Dim xy As New ArrayList<br>        Dim xz As New ArrayList<br>        Dim yz As New ArrayList<br>        Dim pp As pair<br>        Dim s As Integer = 0<br>        For a = 0 To pixels.Length - 1<br>            cc = pixels(a).Split(","c)<br>            p(a).x = cc(0)<br>            p(a).y = cc(1)<br>            p(a).z = cc(2)<br>            pp.x1 = p(a).x<br>            pp.x2 = p(a).y<br>            If Not xy.Contains(pp) Then<br>                xy.Add(pp)<br>                s += 1<br>            End If<br>            pp.x1 = p(a).x<br>            pp.x2 = p(a).z<br>            If Not xz.Contains(pp) Then<br>                xz.Add(pp)<br>                s += 1<br>            End If<br>            pp.x1 = p(a).y<br>            pp.x2 = p(a).z<br>            If Not yz.Contains(pp) Then<br>                yz.Add(pp)<br>                s += 1<br>            End If<br>        Next<br>        For a = 0 To xy.Count - 1<br>            For b = 0 To xz.Count - 1<br>                For c = 0 To yz.Count - 1<br>                    If xy(a).x1 = xz(b).x1 AndAlso xy(a).x2 = yz(c).x1 AndAlso xz(b).x2 = yz(c).x2 Then<br>                        Dim px As pixel<br>                        px.x = xy(a).x1<br>                        px.y = xy(a).x2<br>                        px.z = xz(b).x2<br>                        If Array.IndexOf(p, px) = -1 Then Return -1<br>                    End If<br>                Next<br>            Next<br>        Next<br>        Return s<br>    End Function<br>End Class<br></td>