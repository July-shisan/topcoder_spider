<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class FuelConsumption<br>    Public Function maximalDistance(ByVal velocities As Integer(), ByVal consumptions As Integer(), ByVal fuel As Integer) As Double<br>        Dim i As Integer<br>        Dim travel As Double<br>        Dim lastTravel As Double = 0<br> <br>        For i = 0 To (velocities.Length - 1)<br>            Dim distance As Double<br> <br>            'Find distance per liter for this velocity<br>            distance = velocities(i) / (consumptions(i) / 1000)<br> <br>            travel = distance * (fuel / 1000)<br> <br>            If travel &gt; lastTravel Then<br>                lastTravel = travel<br>            End If<br>        Next<br> <br>        Return lastTravel<br>    End Function<br>End Class<br></td>