<td class="problemText" colspan="8" valign="middle" align="left">
            //Written by Sergey Melnikov<br>import java.util.*;<br>import java.math.*;<br>public class TriangularSubsequence {<br>    public int longest(int[] a) {<br>      Arrays.sort(a);<br>        int res = Math.min(2,a.length);<br>      for (int l = 0; l &lt; a.length-1; l++) {<br>      for (int r = l+1; r &lt; a.length; r++) {<br>        if (a[l]+a[l+1]&gt;a[r]) {<br>          res = Math.max(res, r-l+1);<br>        }<br>      }<br>    }<br>        return res;<br>    }<br> <br>}<br> <br> <br>// Powered by FileEdit<br>// Powered by CodeProcessor<br></td>