<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Text<br> <br>Public Class ChangingString<br>  Public Function distance(ByVal A As String, ByVal B As String, ByVal K As Integer) As Integer<br>  dim i as integer<br>  dim changed(len(A)) as integer<br>  for i=0 to len(A)-1 step 1<br>  changed(i)=0<br>  next i<br>  dim dist as integer<br>  dim t as integer<br>  dist=0<br>  dim maxdist as integer<br>  dim maxi as integer<br>    do while K&gt;0<br>    maxdist=0<br>    maxi=0<br>    for i=1 to len(A) step 1<br>    t=asc(mid(A,i,1))-asc(mid(B,i,1))<br>    if t&lt;0 then t=t* (-1)<br>    if t &gt;= maxdist and changed(i-1)=0 then <br>    maxdist=t<br>    maxi=i<br>    end if<br>    next i<br>    if mid(B,maxi,1)=mid(A,maxi,1) then <br>    changed(maxi-1)=1<br>    mid(B,maxi,1)=chr(asc(mid(A,maxi,1))+1)<br>    K=K-1<br>    else<br>    mid(B,maxi,1)=mid(A,maxi,1)<br>    changed(maxi-1)=1<br>    K=K-1<br>    end if<br>    loop<br>    for i=1 to len(A) step 1<br>    t=asc(mid(A,i,1))-asc(mid(B,i,1))<br>    if t&lt;0 then t= t* (-1)<br>    dist=dist + t<br>    next i<br>    return dist<br>  End Function<br> <br>End Class<br> <br>'Powered by [KawigiEdit] 2.0!<br></td>