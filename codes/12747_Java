<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.Arrays;<br> <br>/**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class PackingSquares {<br>    public long leastArea(int[] a) {<br>        if (a.length == 0) return 0;<br>        int max = 0;<br>        for (int x : a) max = Math.max(x, max);<br>        int lastMaxId = -1;<br>        for (int i = 0; i &lt; a.length; ++i) if (a[i] == max) lastMaxId = i;<br>        int[] na = new int[a.length];<br>        int ptr = 0;<br>        for (int i = 0; i &lt; lastMaxId; ++i) if (a[i] != max) na[ptr++] = a[i];<br>        long res = (1L &lt;&lt; (2 * max)) + leastArea(Arrays.copyOf(na, ptr));<br>        return res;<br>    }<br>}<br></td>