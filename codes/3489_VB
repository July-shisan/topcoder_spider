<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class VLNString<br>        Public Function makeAcronym(ByVal longName As String) As String<br>            Dim i As Integer<br>            Dim s, s1 As String<br>            s = ""<br>            s1 = ""<br>            longName = longName + " "<br>            For i = 0 To longName.Length - 1<br>                If longName.Substring(i, 1) &lt;&gt; " " Then<br>                    s1 = s1 + longName.Substring(i, 1)<br>                Else<br>                    If  Microsoft.VisualBasic.Strings.Trim(s1) &lt;&gt; "" Then<br>                        If Microsoft.VisualBasic.Strings.UCase(s1) &lt;&gt; "AND" And Microsoft.VisualBasic.Strings.UCase(s1) &lt;&gt; "THE" And Microsoft.VisualBasic.Strings.UCase(s1) &lt;&gt; "OF" Then<br>                            s = s + Microsoft.VisualBasic.Strings.UCase(s1.Substring(0, 1))<br>                        End If<br>                        s1 = ""<br>                    End If<br>                End If<br>            Next i<br>            makeAcronym = s<br>        End Function<br>    End Class<br></td>