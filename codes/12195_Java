<td class="problemText" colspan="8" valign="middle" align="left">
            /**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> * @author Egor Kulikov (egor@egork.net)<br> */<br>public class FoxAndMountainEasy {<br>  public String possible(int n, int h0, int hn, String history) {<br>    int min = 0;<br>    int cur = 0;<br>    for (int i = 0; i &lt; history.length(); i++) {<br>      if (history.charAt(i) == 'U')<br>        cur++;<br>      else<br>        cur--;<br>      min = Math.min(min, cur);<br>    }<br>    if (h0 &lt; -min) {<br>      int diff = -min - h0;<br>      n -= diff;<br>      h0 += diff;<br>    }<br>    n -= history.length();<br>    h0 += cur;<br>    if (Math.abs(h0 - hn) &gt; n || Math.abs(h0 - hn) % 2 != n % 2)<br>      return "NO";<br>    return "YES";<br>  }<br>}<br></td>