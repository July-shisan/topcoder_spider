<td class="problemText" colspan="8" valign="middle" align="left">
            /**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class WinterAndPresents {<br>    public long getNumber(int[] apple, int[] orange) {<br>        int max = Integer.MAX_VALUE;<br>        for (int i = 0; i &lt; apple.length; ++i) {<br>            max = Math.min(max, apple[i] + orange[i]);<br>        }<br>        long res = 0;<br>        for (int X = 1; X &lt;= max; ++X) {<br>            int maxApple = 0;<br>            int maxOrange = 0;<br>            for (int i = 0; i &lt; apple.length; ++i) {<br>                maxApple += Math.min(X, apple[i]);<br>                maxOrange += Math.min(X, orange[i]);<br>            }<br>            int total = X * apple.length;<br>            int minApple = total - maxOrange;<br>            res += maxApple - minApple + 1;<br>        }<br>        return res;<br>    }<br>}<br></td>