<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class RollingDiceDivTwo<br>        Public Function minimumFaces(rolls As String()) As Integer<br>            Dim i As Integer, j as integer, res As Long, s as long<br>            Dim dice(rolls(0).length()-1) as integer <br>            Dim cur(rolls(0).length()-1) as integer <br>            <br>            for j = 0 to cur.length()-1<br>              dice(j)  = 1<br>            next<br>            <br>            for i = 0 to rolls.length - 1<br>              s = 0 <br>              for j = 0 to rolls(i).length()-1<br>                cur(j) = CInt(CStr(rolls(i)(j)))<br>              next<br>              Array.Sort(cur)<br>              for j = 0 to cur.length()-1<br>                dice(j) = Math.Max(cur(j), dice(j))<br>              next<br>            next<br>            s = 0 <br>            for j = 0 to cur.length()-1<br>              s = s + dice(j)<br>            next<br>            Return s<br>        End Function<br>    End Class<br></td>