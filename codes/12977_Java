<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.Arrays;<br> <br>/**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> * @author nasko<br> */<br>public class EllysSubstringSorter {<br>    public String getMin(String S, int L) {<br> <br>        String t = S.substring(0,0+L);<br>        char[] q = t.toCharArray();<br>        Arrays.sort(q);<br>        String best = new String(q) + S.substring(L);<br>        for(int i = 1; i &lt; S.length(); ++i) {<br> <br>            if(i+L &lt;= S.length()) {<br>                String m = S.substring(i,i+L);<br>                char[] p = m.toCharArray();<br>                Arrays.sort(p);<br>                String temp = S.substring(0,i) + new String(p) + S.substring(i+L);<br>                if(best.compareTo(temp) &gt; 0) best = temp;<br>            }<br>        }<br>        return best;<br> <br>    }<br>}<br></td>