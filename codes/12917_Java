<td class="problemText" colspan="8" valign="middle" align="left">
            /**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class PowerOfThree {<br>    public String ableToGet(int x, int y) {<br>        while (x != 0 || y != 0) {<br>            x = Math.abs(x);<br>            y = Math.abs(y);<br>            if (x % 3 == 1)<br>                --x;<br>            else if (x % 3 == 2)<br>                ++x;<br>            else if (y % 3 == 1)<br>                --y;<br>            else if (y % 3 == 2)<br>                ++y;<br>            else {<br>                return "Impossible";<br>            }<br>            if (x % 3 != 0 || y % 3 != 0) {<br>                return "Impossible";<br>            }<br>            x /= 3;<br>            y /= 3;<br>        }<br>        return "Possible";<br>    }<br>}<br></td>