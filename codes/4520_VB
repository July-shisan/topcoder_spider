<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Public Class TimeCard<br> <br>    Public Function leave(ByVal time As String()) As String<br>        Dim WkMins As Integer<br>        Dim RemMins As Integer<br>        Dim i As Integer<br>        Dim start As Date<br>        Dim finish As Date<br> <br>        Dim result As String<br> <br>        For i = 0 To time.Length - 3 Step 2<br>            start = CDate(time(i))<br>            finish = CDate(time(i + 1))<br>            If start &gt; finish Then<br>                finish = DateAdd(DateInterval.Day, 1, finish)<br>            End If<br>            WkMins = WkMins + DateDiff(DateInterval.Minute, start, finish)<br>        Next<br> <br>        RemMins = 2400 - WkMins<br>        If RemMins &lt; 0 Then<br>            result = "ABOVE 40"<br>        ElseIf RemMins &gt; 1200 Then<br>            result = "BELOW 40"<br>        Else<br>            start = CDate(time(time.Length - 1))<br>            finish = DateAdd(DateInterval.Minute, RemMins, start)<br>            result = finish.ToString("hh:mm,tt")<br>      result = result.ToLower<br>        End If<br> <br> <br>        Return result<br> <br>    End Function<br> <br>End Class<br></td>