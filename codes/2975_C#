<td class="problemText" colspan="8" valign="middle" align="left">
            using System;<br>using System.Text;<br>using System.Text.RegularExpressions;<br>using System.Collections;<br>using AL = System.Collections.ArrayList;<br>using HT = System.Collections.Hashtable;<br><br>public class FileFilter {<br>  public string[] filter(string[] files, string[] extensions) {<br>    string[] r = new string[files.Length];<br>    for (int i=0;i&lt;files.Length;i++)<br>    {<br>      string ft = files[i].ToUpper();<br>      bool ok = false;<br>      foreach (string e in extensions)<br>      {<br>        if (ft.EndsWith("." + e.ToUpper()))<br>          ok = true;<br>      }<br>      r[i] = ok ? "ALLOW" : "DENY";<br>    }<br>    return r;<br>  }<br>  static AL nAL { get { return new AL(); } }<br>  static HT nHT { get { return new HT(); } }<br>  static void WL( string s, params object[] o ) { <br>#if KV_LOCAL<br>    Console.WriteLine( s, o ); <br>#endif<br>  }<br>  static int INT( string o ) { return int.Parse( o ); }<br>  static int[] INT( string[] s ) { int[] r = new int[s.Length]; for (int i=0;i&lt;s.Length;i++) { r[i] = INT(s[i]); } return r; }<br>  static string str(object o)<br>  {<br>    if (o == null) return "null";<br>    if (o is ArrayList)<br>      o = ((ArrayList)o).ToArray();<br>    if (o is Array)<br>    {<br>      Array a = o as Array;<br>      StringBuilder b = new StringBuilder();<br>      for (int i=0;i&lt;a.GetLength(0);i++)<br>      {<br>        if (i != 0) b.Append(";");<br>        b.Append(a.GetValue(i));<br>      }<br>      return b.ToString();<br>    }<br>    else<br>      return o.ToString();<br>  }<br><br><br>}<br><br><br>// Powered by FileEdit<br>// Powered by CodeProcessor<br></td>