<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.Arrays;<br> <br>/**<br> * Built using CHelper plug-in<br> * Actual solution is at the top<br> */<br>public class TaroFriends {<br>    public int getNumber(int[] coordinates, int X) {<br>        Arrays.sort(coordinates);<br>        int res = coordinates[coordinates.length - 1] - coordinates[0];<br>        for (int lastRight = 0; lastRight + 1 &lt; coordinates.length; ++lastRight) {<br>            int left = Math.min(coordinates[0] + X, coordinates[lastRight + 1] - X);<br>            int right = Math.max(coordinates[lastRight] + X, coordinates[coordinates.length - 1] - X);<br>            res = Math.min(res, right - left);<br>        }<br>        return res;<br>    }<br>}<br></td>