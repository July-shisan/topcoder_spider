<td class="problemText" colspan="8" valign="middle" align="left">
            /*/*///*/*/ Powered by radedit!            /*/*/*/*/*/ ;<br>/*/*//*/*/  import java . util .           /*/*/*/*/*/ ;<br>/*/*//*/*/  import java . lang . reflect . /*/*/*/*/*/ ;<br>public class AntiMatter {<br>   boolean ddd = false ;<br>long gcd(long a, long b) {<br>   if (b == 0) return a ;<br>   return gcd(b, a % b) ;<br>}<br>  public String unstable(int[] xform) {<br>    int i, j, k ;<br>    int g = 0 ;<br>    for (i=0; i&lt;xform.length; i++) {<br>      for (j=i+1; j&lt;xform.length; j++) {<br>  int d = Math.abs(xform[i]-xform[j]) ;<br>  if (d &gt; 0) {<br>    if (g &gt; 0) {<br>      g = (int)gcd(d, g) ;<br>    } else {<br>      g = d ;<br>    }<br>  }<br>      }<br>    }<br>    long p = 0 ;<br>    int m=1 ;<br>    for (i=9999; i&gt;0; i--) {<br>      if (g != 0 &amp;&amp; i % g == 0) {<br>  p = p + m ;<br>      }<br>      m++ ;<br>    }<br>    p += m / 2 ;<br>    p *= 2 ;<br>    String s = "" + p ;<br>    while (s.length() &lt; 8)<br>      s = "0" + s ;<br>    if (s.length() == 9) {<br>      return "1.00000000" ;<br>    }<br>    return "." + s ;<br>  }<br> <br>  static void pp(Object o) { System.out.println(o) ; }<br>}<br></td>