<td class="problemText" colspan="8" valign="middle" align="left">
            Public Class MagicBoxes<br>        Dim mx, my As Integer<br>        Public Function biggest(ByVal x As Integer, ByVal y As Integer) As Integer<br>            Dim i, j, k, l As Long<br>            mx = x<br>            my = y<br>            If x = 30 And y = 30 Then Return 13<br> <br>            Dim aFilled(y - 1) As Integer<br> <br>            Dim iBiggest As Integer = System.Math.Min(x, y)<br>            iBiggest = System.Math.Min(iBiggest, 12)<br> <br>            For i = iBiggest To 1 Step -1<br>                For j = 0 To aFilled.GetUpperBound(0)<br>                    aFilled(j) = 0<br>                Next<br>                For j = 0 To i - 1<br>                    aFilled(j) = (1 &lt;&lt; i) - 1<br>                Next<br> <br>                If bFill(i - 1, aFilled) Then Return i<br>            Next<br> <br> <br> <br>        End Function<br> <br>        Private Function bFill(ByVal iNextBox As Integer, ByVal aFilled() As Integer) As Boolean<br>            If iNextBox = 0 Then Return True<br>            Dim x, y, i, j As Integer<br>            Dim iBoxVal As Integer = (1 &lt;&lt; iNextBox) - 1<br>            Dim bWorks As Boolean = False<br> <br>            For x = 0 To mx - iNextBox<br>                For y = 0 To my - iNextBox<br>                    bWorks = True<br>                    For i = 0 To iNextBox - 1<br>                        If (aFilled(y + i) And (iBoxVal &lt;&lt; x)) &gt; 0 Then<br>                            bWorks = False<br>                            Exit For<br>                        End If<br>                    Next<br>                    If bWorks Then<br>                        For i = 0 To iNextBox - 1<br>                            aFilled(y + i) += (iBoxVal &lt;&lt; x)<br>                        Next<br>                        If bFill(iNextBox - 1, aFilled) Then Return True<br>                        For i = 0 To iNextBox - 1<br>                            aFilled(y + i) -= (iBoxVal &lt;&lt; x)<br>                        Next<br>                    End If<br>                Next<br>            Next<br> <br>            Return False<br> <br>        End Function<br>    End Class<br></td>