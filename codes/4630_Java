<td class="problemText" colspan="8" valign="middle" align="left">
            import java.util.*;<br> <br>public class RectangleError<br>{<br>  public double bottomRange(double topMin, double topMax, double leftMin, double leftMax, double rightMin, double rightMax)<br>  {<br>    double mindiff = 0;<br>    <br>    if ( leftMin &lt;= rightMax &amp;&amp; rightMin &lt;= leftMax ) {<br>      mindiff = 0.;<br>    }<br>    else {<br>      mindiff = Math.min( Math.abs( leftMax - rightMin ), Math.abs( rightMax - leftMin ) );<br>    }<br>    <br>    double maxdiff = Math.max( Math.abs( leftMax - rightMin ), Math.abs( rightMax - leftMin ) );<br>    <br>    double maxLen = Math.sqrt( maxdiff * maxdiff + topMax * topMax );<br>    double minLen = Math.sqrt( mindiff * mindiff + topMin * topMin );<br>    <br>    return maxLen - minLen;<br>  }<br>}<br> <br>//Powered by [KawigiEdit]<br></td>