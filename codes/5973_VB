<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br>Imports System<br>Imports System.Collections<br>Imports System.Math<br> <br>Public Class MooresLaw<br>    Dim iYears As Integer<br>    Public Function shortestComputationTime(ByVal years As Integer) As Double<br>        Dim i, j, k, l As Integer<br>        iYears = years<br> <br>        Dim dMin, dMax As Double<br>        Dim dTry(1) As Double<br>        Dim dRes(1) As Double<br>        dMin = 0<br>        dMax = years<br> <br>        For i = 0 To 1000<br>            dTry(0) = (dMin * 2 + dMax) / 3<br>            dTry(1) = (dMin + dMax * 2) / 3<br>            dRes(0) = dTime(dTry(0))<br>            dRes(1) = dTime(dTry(1))<br>            If dRes(0) &lt; dRes(1) Then<br>                dMax = dTry(1)<br>            Else<br>                dMin = dTry(0)<br>            End If<br>        Next<br>        Return dTime((dMax + dMin) / 2)<br> <br>    End Function<br> <br>    Private Function dTime(ByVal dWait As Double) As Double<br>        Dim dSpeed As Double = 2 ^ (dWait / 1.5)<br>        Return dWait + iYears / dSpeed<br>    End Function<br>End Class<br></td>