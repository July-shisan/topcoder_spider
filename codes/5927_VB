<td class="problemText" colspan="8" valign="middle" align="left">
            Imports Microsoft.VisualBasic<br> <br>Public Class TestCurve<br>        Public Function determineGrades(ByVal scores As Integer()) As Integer()<br>            Dim max, i As Integer<br>            Dim aa(scores.Length - 1) As Integer<br>            max = 0<br>            For i = 0 To scores.Length - 1<br>                If scores(i) &gt; max Then<br>                    max = scores(i)<br>                End If<br>            Next<br>            For i = 0 To scores.Length - 1<br>                aa(i) = Int((scores(i) / max) * 100)<br>            Next<br>            Return aa<br> <br>        End Function<br>    End Class<br></td>