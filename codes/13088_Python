<td class="problemText" colspan="8" valign="middle" align="left">
            import math,string,itertools,fractions,heapq,collections,re,array,bisect,random<br> <br>class AmebaDiv1:<br>    def count(self, X):<br>        x = set()<br>        for i in X:<br>            x.add(i)<br>        for i in X:<br>            s = i<br>            for j in X:<br>                if s == j:<br>                    s *= 2<br>            if s in x:<br>                x.remove(s)<br>        return len(x)<br> <br> <br> <br> <br> <br> <br> <br> <br> <br> <br>#Powered by KawigiEdit-pf 2.3.0!<br></td>